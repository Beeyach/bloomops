import '@/app/pages.css';

// Preview reuses the Page reader's typography/icon sizing without mounting
// its interactive workspace tree or any internal shell.
export default function PreviewLayout({ children }) { return children; }
