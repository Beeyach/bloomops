You are implementing PERF3 for Beeyach/bloomops on branch perf/perf3-rsc-completion.

Read docs/phases/PERF3.md completely first. It is binding.

Exact base for this phase is 62bcf019226db1e7e939fbcffc8909f41fbd577a. Confirm your branch descends from it before making application changes.

Context from live signed-in staging after merged PERF2:
- Home: ~1.05-1.10 s warm click-to-visible, ~0.27 s first-response proxy, ~0.81 s remaining
- Systems: ~1.02-1.04 s, ~0.27 s first-response proxy, ~0.76 s remaining
- Work: ~0.87-0.89 s, ~0.61 s after response begins
- Team: ~0.84-0.92 s, ~0.60 s after response begins
- Social: ~0.86-0.88 s, ~0.60 s after response begins
- Clients: ~0.85-0.88 s, ~0.58 s after response begins
- Ads: ~0.69-0.83 s, ~0.50 s after response begins
Three warm cycles were nearly identical. No console errors. Current staging data is sparse.

Do not assume the post-first-response delay is browser rendering. The first bytes may be an early RSC stream while server/D1 work continues. Your first job is to split click -> first byte -> final body byte -> DOM-complete marker -> two animation frames and correlate that with server authorization, D1 invocations, projection completion, serialization/stream flush, and React commit.

Home and Systems are the primary targets. Trace them end-to-end before choosing a fix. Then determine whether the same cause explains the other top-level routes.

Do not do another generic query-count cleanup. PERF1 and PERF2 already proved that fewer statements or fewer native binding invocations do not automatically make the live app fast. Fix only a proven critical-path cost.

Security is non-negotiable. Every protected request must continue to use current database truth. Preserve immediate next-request revocation for session, membership, assignments, capabilities, visibility and Client contact linking. Preserve tenant isolation and 401/403/404 existence hiding. Do not enable long-lived Better Auth/session caching, cross-request authorization caching, stale protected route caching, or protected prefetch reuse that could show data after revocation.

Do not hide latency with loading UI and call it fixed. Measure completed destination content.

Preserve PERF2 representative and exact 3x stress fixtures. A fix must remain bounded at both scales.

Use safe instrumentation only. Do not log SQL text, bind values, cookies, tokens, user content, row payloads or private IDs. Temporary staging diagnostics must be removed before final implementation handoff unless they are intentionally retained as safe audited observability.

You may consult current official Cloudflare, D1, Next.js, React, OpenNext and Better Auth documentation where needed. Prefer direct official sources. Do not mutate production. Do not send real email automatically.

Run all verification required by docs/phases/PERF3.md, including full tests/builds, runtime/auth, zero-to-current, browser acceptance, representative/stress performance, revocation/security, syntax and diff checks.

Before handoff:
1. Write docs/PERF3_RESULTS.md with exact evidence and limitations.
2. Update docs/BUILD_STATE.md and docs/INDEX.md.
3. Update durable architecture docs only if architecture changed.
4. Delete PERF3_CODEX_PROMPT.txt from the implementation branch.
5. Ensure working tree is clean.
6. Push branch.
7. Open a PR against main.
8. Leave PR OPEN and UNMERGED for independent audit.

Final report must include exact base SHA, exact final head SHA, measured root cause, first-byte/final-body/DOM timing, Home and Systems before/after, other top-level route results, representative/stress results, security evidence, complete verification, infrastructure/schema/dependency changes if any, and remaining uncertainty.

D2 remains blocked. Do not begin D2.