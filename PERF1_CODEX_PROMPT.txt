You are implementing PERF1 Navigation Performance Hardening for Beeyach/bloomops.

Start from the current branch `perf/navigation-hardening`, which was created from exact closed D1 merge SHA `e5bc81630c47fb6eb51e9bdc6be1687947683643`.

Read these completely before materially editing code:

1. `AGENTS.md`
2. `docs/BUILD_STATE.md`
3. `docs/RELEASE_D.md`
4. `docs/phases/PERF1.md`
5. `docs/phases/D1.md`
6. `docs/PRODUCT_SPEC.md`
7. `docs/DOMAIN_MODEL.md`
8. current internal/portal shell, navigation, auth, membership and authorization implementation
9. current Home, Clients, Work, Social, Systems and Team pages/read models
10. current OpenNext/Cloudflare configuration, staging verification, browser and runtime harnesses

Then execute `docs/phases/PERF1.md` exactly.

Critical context:

- The user experiences roughly 2 seconds of visible delay per tab/click on staging.
- D1 independent re-audit found localhost warm navigation was much faster: Work ~76.75 ms median, Social ~61.40 ms, Systems ~93.65 ms after the D1 loading-boundary fix.
- Therefore do not assume the problem is a single slow page. Reproduce and attribute the deployed/request path difference.
- Measure before optimizing. Build a repeatable timing harness first.
- Preserve current-truth authorization. Membership suspension/removal, assignment revocation, visibility restriction and Client contact unlinking must still revoke access on the next protected request.
- Do not enable stale cross-request permission caches just to make numbers look better.
- Do not treat spinners/skeletons as the fix.
- Do not reintroduce the D1 JavaScript-dependent streaming/loading defect.
- Prefer request-local deduplication, safe parallelization, query consolidation, removing proven duplicate reads and safe navigation/prefetch improvements.
- Treat session lookup, membership lookup, actor loading, page read queries, OpenNext RSC behavior, Cloudflare/D1 regional effects, prefetch and visible commit timing as hypotheses to measure, not assumptions.
- Do not start D2 or add new Systems functionality.

Before/after evidence is mandatory. Include sample count, warmups, median and range/percentile for representative top-level routes and at least one detail route. If you cannot authenticate against remote staging safely, say so and do not fabricate remote timing claims. Build the strongest safe local/deployed-path approximation available and leave the user-visible staging confirmation for post-merge if necessary.

Run the full verification required by PERF1. Keep package/lock/schema/migrations/config unchanged unless evidence proves a narrowly scoped change is necessary. Do not run broad dependency upgrades or `npm audit fix`.

When finished:

- update `docs/BUILD_STATE.md`
- update `docs/INDEX.md` so PERF1 is the active task and D2 remains blocked
- update any durable architecture docs only if behavior actually changed
- delete `PERF1_CODEX_PROMPT.txt` before the final implementation commit so it has no net diff against main
- keep the working tree clean
- push `perf/navigation-hardening`
- open a PR against `main`
- leave the PR open and unmerged for independent audit

Final report must include exact base SHA, exact final head SHA, proven root cause(s), before/after timing table, query/round-trip changes, security/revocation verification, test/build/runtime/browser results, schema/dependency/config status and remaining uncertainty.

Do not merge the PR.