# P3C3B reply and stop review preview

Actual captures of the built local Worker with isolated synthetic D1/R2 and intercepted provider traffic. No live email or mailbox read. The Bloomsi lockup, garden identity, compact icons, labelled fields and responsive shell use the existing design. Full-page captures include the fixed mobile navigation at its viewport position; the page scrolls underneath it.

| View | Capture |
| --- | --- |
| Ready to review | [Desktop](review-1440.png), [Phone](review-390.png) |
| Saved reply hold | [Desktop](held-1440.png), [Phone](held-390.png) |
| Permanent stop and evidence | [Phone](stopped-390.png) |
| Conversations in Overview | [Phone](overview-390.png) |
| Interrupted request with keyboard focus | [320px](error-320.png) |
| Another receipt remains disabled | [320px](disabled-320.png) |

Verification: 100 distinct focused SQL/domain/provider tests, 16 native D1 checks, 41 built-browser checks at 1440/1024/768/390/320px, final Cloudflare build and populated/fresh/repeat migration proof. Keyboard acknowledgement/check/stop, busy/cooldown controls, error focus, denied roles/workspaces, permanent stop and visible status labels pass. No runtime browser errors. Independent review and the live acceptance gate are recorded in [BUILD_STATE](../../BUILD_STATE.md).

The check reads only the original accepted conversation's headers. Every incoming observation or unresolved check holds outreach; an empty later check cannot clear it. Human stops retain a reason and evidence permanently. No body classification, automatic resume, scheduling, follow-up or Results claim. Live mailbox acceptance remains owner-gated.

## Provider-returned identity follow-up

The compatibility fix retains this UI.72 affected provider/domain/workerd tests,17 native D1 checks and41 final built-browser checks pass with a synthetic accepted message whose returned RFC ID differs from the submitted receipt. Desktop and phone captures in the private `provider-anchor-fix/browser/` evidence were inspected. No new live mailbox read during fix verification. BUILD_STATE records review and the pending owner-authorized live verification separately.
