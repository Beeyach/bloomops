{
  "checks": [
    "Cancelling a query change preserves the typed draft",
    "Typed field persists only its ID, field, baseline and value",
    "Reload offers recovery without displaying stored values",
    "Recovery sends only bounded IDs and context before displaying values",
    "Explicit recovery restores raw values without submitting",
    "Recovered fields and actions fit viewport 1440",
    "Recovered fields and actions fit viewport 390",
    "Recovered fields and actions fit viewport 320",
    "Confirmed save clears the current recovery copy",
    "New browser context recovers incomplete input",
    "Invalid recovered input remains editable and is not sent",
    "Corrected recovered input saves through the canonical API",
    "Back and forward navigation preserves the unsaved value",
    "Recovered baseline cannot overwrite a concurrent edit",
    "Interrupted committed save is confirmed without another write",
    "Ten-copy recovery keeps its source without exceeding capacity",
    "Saving at capacity retires only the verified source",
    "Other users and workspaces have no recovery prompt",
    "Missing-record validation never reveals the stored value",
    "Native D1 recovery supports200 records with fewer than100 bindings",
    "Native endpoint refuses an identity mismatch",
    "Anonymous recovery is denied",
    "Unavailable storage warns while retaining the mounted value",
    "Discovered revocation hides draft values and clears the original scope",
    "Sheet writes reject a different initiating user without changing the record",
    "Two tabs retain independent field values",
    "Workspace switching invalidates the old tab even when the epoch write fails",
    "Returning to the authorized workspace offers its separate drafts",
    "Opening a shared page invalidates the previous workspace sheet",
    "Legacy rail logout clears copies and invalidates another tab even when sign-out fails",
    "Explicit logout removes all sheet copies across tabs",
    "Logout clears mounted recovered and typed values",
    "No provider egress"
  ]
}
