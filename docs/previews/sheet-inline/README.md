# Inline prospect edit recovery

The owner reported that Review change and confirmation prompts still made editing feel unreliable. Read-only inspection of the open owner tab found a restored Platform draft (Shopify, originally based on Unknown) while the current saved value was Webflow. The old generic Retry could never replace that newer field because it retained the original comparison; the global review also made another cell ask to discard it. [Observed state](reported-state.json). No action was taken in that owner tab.

Ordinary failures and recovered edits now stay beside their cell, displaying the attempted value. Retry rechecks the canonical record; a real conflict shows the current saved value with Use my value or Keep saved. Use my value still compares against that displayed saved value and refuses a newer concurrent change. Another cell can save without discarding a failed field. Text entry is preserved when moving to another field/view, and hidden/off-page drafts remain accessible. Recovery storage now combines those independent fields without losing original baselines or incomplete input.

Older copies are collapsed behind Unsaved edits from earlier visits. Restore checks current access before exposing fields, skips already-committed values without another write, and returns pending values to their cells. Snapshot retirement preserves concurrent writers and remains safe when the copy limit blocks durable handoff. Bulk/paste keeps its explicit Apply step, actual page departure keeps its unsaved warning, and imports/outreach/client-handoff gates are unchanged. This change does not automatically submit recovered values or overwrite another editor.

## Final local verification

- Final OpenNext build and35 focused sheet/draft Node tests pass.
- [37 new inline recovery checks](inline-checks.json): multiple failed cells, independent subsequent saves, off-page access, reload/collapsed copies, recovered conflicts, intervening writes, guarded replacement/Undo, Keep saved, lost replies without duplicate writes, parked text, failed clears, already-saved copies, full-copy-store preservation/retirement, denied authority and desktop/phone controls.
- [32 updated direct-save regressions](save-checks.json) pass with inline failure/recovery expectations, including failures/lost responses/concurrency, custom/text edits, Undo, keyboard/six widths, profile return and creation warning boundaries.
- [21 quiet-save regressions](smooth-checks.json) pass, including held reads, stale responses, query/count guards and guarded Undo.
- All90 browser checks run against the final built Worker using independent synthetic D1/R2 on8807. Provider egress is blocked; zero provider calls. A recovery test initially read the page before its asynchronous access check completed; it now waits for the checked copy handoff.
- Home/Clients103kB, Portal112kB and Search111kB rounded first-load estimates are unchanged; Prospecting122→123kB. No backend query change or server-latency claim. No new migration/dependency/workflow change.

[Desktop inline failure](desktop.png) and [phone inline failure](phone.png) were inspected. Full-page phone captures include fixed navigation at its capture position.

One fresh Sol High review found two edge cases: recovery actions could submit an older queued value while a text editor was open, and Keep saved could reveal a stale local value. Recovery actions are now hidden for the active editor. Keep saved reconciles the compared canonical value/revision immediately, preserves any newer known row, and refreshes quietly. New held-read and typed-replacement regressions cover both. The single permitted focused re-review confirmed the editor fix and identified a remaining same-row revision issue: a later single-field save advanced the row revision without refreshing other canonical fields. Direct confirmation now merges the profile read for that guarded write before applying the field/revision. The held-read regression now saves another field on the same row before Keep saved. The final correction passes all90 browser checks on the rebuilt Worker, including the held-read same-row case. It was locally verified after the final permitted re-review; no additional independent signoff is claimed. **Live in [staging](https://staging.ops.gobloomwired.com/prospecting)** on runtime `f22a0b9eab6967b032f2c8c047d7e310f22a4461`; [workflow34893504562](https://github.com/Bloomwired/bloomops/actions/runs/34893504562) succeeded. Seven deployed checks pass: version/health45, one deliberately simulated503 retained inline, another real field save without discard, real Retry/readback, real Undo restoring freshly captured QA fields/sources, phone44px/no overflow and restored desktop. Zero confirmation dialogs. Only the designated fictional QA record was edited; expected edit/undo history and newer revision/time remain. Desktop/phone captures were inspected. Local Worker8807 is stopped. [Deployed desktop](staging-desktop.jpg) and [deployed phone](staging-phone.jpg). [Live checks](staging-checks.json). PR73 remains draft/unmerged. N2 is unstarted. Original LTB/voices, root D2/unrelated work, production/DNS, real imports/outreach and paid/video work remain untouched.

The live failure was a one-shot fetch503 simulation confined to the task-owned tab; subsequent save, retry, readback and Undo used the deployed API. The task did not restore, discard or clear any user recovery copy or operate the owner tab. Two pre-existing copies disappeared between the initial snapshot and post-deployment reload while other tabs were active; the precondition comparison detected this before test mutations. Their byte-for-byte preservation cannot be claimed from this live session. Independent synthetic local checks verify recovery retention/collapse/concurrent-writer behavior. Temporary fetch/confirm wrappers were restored after acceptance.
