# N1 Save reliably and find records

Owner direction: [NEXT_PHASES](../NEXT_PHASES.md). Continue from the accepted sheet/P5C slice. Keep the staged sheet and draft PR64 intact; use `feat/n1-sheet-save-recovery` in its own worktree. No production, DNS, real imports/outreach, paid audits or video work.

## N1A — Retry failed sheet saves

The first bounded slice preserves reviewed values after a failed or partially successful sheet save. Keep failed fields and their original comparison values in the mounted review, identify each error beside its field, and offer an explicit retry. Confirm already-applied values after a lost response without writing again. Recheck current profile/revision on every retry and refuse to overwrite a newer field. Preserve undo receipts for confirmed successes across partial retries. Undo and save status must describe the action accurately.

Pending previews require an explicit discard before another edit or sheet query/view replaces them. Ordinary link navigation and browser reload warn about unsaved values; this slice does not claim durable recovery after accepting a leave prompt, closing a crashed browser or losing the component. No draft is written into browser storage and no automatic retry is introduced. Full recovery across navigation/session boundaries, including client-side history navigation, remains N1B.

Acceptance: actual built-Worker checks with synthetic isolated records cover offline/503 failure, mixed success, delayed saves and double submission, retry, committed-but-lost responses, concurrent edits, denied access, undo retention, discard guards, desktop/mobile layouts and no provider egress. Existing focused sheet tests and Worker build must pass. Compare unrelated route bundles and request counts with the accepted build; do not infer a production speed improvement. Apply the configured selective independent review policy.

No migration, server-side authority change, deployment or PR64 merge is required for N1A. Record local acceptance separately from staging. Later N1 work covers broader save/recovery and bounded permission-aware global search.

## N1B — Browser recovery for sheet drafts

Use a separate worktree/branch `feat/n1b-sheet-drafts`, preserving PR64/65 and staging. Retain the typed cell or pending reviewed fields in browser-local storage, scoped to the original user/workspace. Each mounted sheet writes its own draft ID so another tab cannot overwrite its copy. Keep at most ten copies per scope for seven days and cap each at200 fields/256KiB; report unavailable/full storage without pretending the copy is safe.

On a later visit, offer recovery without rendering stored field values first. An explicit read-only, bounded server check must confirm current Owner/Admin prospecting access, matching user/workspace and all referenced records before revealing values. Recovery restores original comparison values and query, permits correction of incomplete input, and never submits automatically. Every recovery-capable sheet save/undo also carries the initiating user ID, checked against the current actor; every save still validates current values/revision. Existing callers without that new context field retain their API behavior. Lost responses can be confirmed without another write. Missing records block the entire candidate rather than exposing unvalidated content.

Clear sheet copies on explicit logout; invalidate mounted sheets on account/workspace changes and stop future writes from disposed/invalidated controllers. Returning to the original authorized workspace may recover its distinct copy. Discovered revocation clears that scope. Already transmitted requests may finish; no new writes start after departure or invalidation. Browser back/forward, navigation, reload and a new browser context using the same storage must recover through the same review. This is browser-local recovery, not cross-device sync or guaranteed survival when browser storage is removed. Undo receipts remain in the mounted session.

Acceptance: bounded/invalid/expired storage, independent writers, logout/epoch invalidation; native permission, stale-membership, foreign/missing-record and no-write checks for recovery; built-Worker typed/review recovery, invalid input correction, partial/lost responses, concurrent values, navigation/history/reload/new-context, logout/workspace changes/revocation, storage failures and desktop/mobile. Preserve unrelated route query counts and measure shared bundle impact. Follow the one Sol High review and at most one focused re-review policy. No migration, deployment, real import/outreach, paid auditing or video work.

## N1C — Permission-aware workspace search

Continue from merged/staged `9763650` in the separate `feat/n1c-global-search` worktree. Add a labelled Search entry in the existing internal/Prospecting shell and a compact entry on phones. A full-page search supports prospects, clients, Tasks (canonical Actions), Pages and Files in the selected workspace. Portal search offers shared Pages and downloadable Files; there is no dedicated portal client/task detail surface to link. No indexing service or external AI, and no prospect/contact/audit/document-body snippets.

Require explicit submission of2–120 characters. Read-only POST keeps terms out of URL history. An all-types query returns at most5 authorized results per type, with a per-type view of20 and bounded page navigation. Use literal substring matching, stable name/ID order, current membership/role and canonical resource predicates before LIMIT. A single read-only D1 batch includes an authority guard and the selected type queries. Bind initiating user/workspace to the request; reject replaced identity or workspace. No global/unfiltered counts, cached results or persistent recent queries. Missing grants/assignments/contact links and restricted/inherited access must behave exactly like canonical records.

Use meaningful coloured type icons, headings, spacious result rows, existing prospect favicon/garden artwork, real canonical links and explicit file-download semantics. No metadata chains. Keyboard Tab/Enter, loading skeletons, useful empty/errors, pagination and narrow layouts are required. Editing a query clears old results and cancels its request; late responses cannot replace a newer query. Workspace/logout lifecycle changes invalidate results, and hidden/back-forward-restored pages clear results until another explicit search. Following a result rechecks canonical access.

Acceptance: focused real-SQLite permission matrices for all internal roles and client portal, workspace boundaries, revoked membership/role/assignment/contact/grants, inherited-page denial and file-parent visibility; proof of no table writes, literal wildcard/invalid inputs, stable bounds and no private-body matching. Built-Worker/native D1 verifies real API shape/bind limits, keyboard/results/open/download, delayed/offline/stale-response/scope changes, desktop1440/1024/768/mobile390/320. Compare unrelated Home/Clients/Work request/query/bundle load against merged main and measure bounded search load. Use one Sol High review and at most one focused re-review. No migration unless evidence establishes an index is needed; no staging/production deployment, real imports/outreach or video work in this slice.

## N1D — Recover full-page prospect profile edits

Continue the full agreed roadmap from the accepted sheet correction; preserve independent search PR67 and sheet PR68. This implementation worktree starts from PR68. Existing profile editors warn before leaving but lose their values after navigation/reload. Extend their current forms and canonical save path with bounded browser-local recovery, explicit retry and honest conflict handling. Identity/contact, assessment, evidence and outreach-draft sections remain the existing structured profile; no sending or audit execution is introduced.

Retain the user's original values, original revision and explicitly edited source controls in independent per-tab copies scoped to user, workspace and prospect. Limit copies to ten per prospect, seven days and256KiB each. Offer only section/count/date metadata initially. An explicit read-only server check must confirm the initiating user, selected workspace and current prospect-management authority before stored field values appear. Recover incomplete input for correction; never auto-submit it or silently accept a newer revision. Canonical saves bind the initiating user while preserving existing API callers. A failed/lost reply retains the draft; retry checks current values first and may confirm already-saved values without another write. Conflicts keep the draft available and offer a deliberate review of saved values, without silently overwriting either version or reasserting source verification.

Invalidate mounted editors and pending reads on logout/account/workspace changes. Clear local profile copies on logout; disposed writers cannot recreate them. Conditional removal must not erase a newer copy written by another tab. Navigation/history/reload recovery, expiry/size limits, storage failure, wrong-user/workspace/revoked access, source metadata, concurrent edits and lost responses need meaningful focused/native/browser checks. Preserve the sheet and existing Pages behavior. Broader client/new-record forms and Pages recovery still require inventory before N1 is called complete. No new migration unless supported by implementation evidence. Follow one Sol High review and at most one focused re-review; keep video work/tests, paid auditing and live outreach paused.

## Remaining N1 inventory — inspected after N1D

These are remaining requirements, not completion claims or permission to activate paused systems. Existing domain actions, role rules and mutation receipts must be reused.

| Existing surface | Observed implementation | Remaining acceptance |
| --- | --- | --- |
| New prospect (`ProspectProfile.jsx`, `createProspect`) | Mounted fields and request ID; leave warning; canonical creation fingerprint/receipt. | Retain original creation identity and incomplete input across navigation/reload; authority-bound recovery and initiating-user creation check; lost-response replay without a second prospect. This is the next bounded implementation. |
| Pages (`PageEditor.jsx`, `page-drafts.mjs`) | Serial autosave; tab session-storage copy; revision conflict and download/latest review. Restore schedules save immediately; current lifecycle lacks the shared invalidation signal. | Check current bound edit authority before recovery, preserve original revision, stop stale/disposed writers, clear/invalidate copies on logout/revocation/switch; confirm lost replies without a duplicate save. Preserve the mature rich editor, nested pages and sharing. |
| Client creation (`ClientForm.jsx`, `createClient`) | Mounted input; canonical client/contact/activity transaction; no durable creation request identity in this path. | Recover drafts and prevent duplicate clients after lost replies; bind initiating user/workspace. Continue creating Draft clients without invitations or activation. |
| Client identity/contact (`ClientOverview.jsx`, `ClientContacts.jsx`) | Mounted dialog values and canonical mutations; no expected-revision field in current requests. | Explicit concurrency/compare guards and recoverable edits before claiming no silent overwrite. Preserve primary contacts, portal links and lifecycle. |
| Project/content forms (`ProjectForm.jsx`, `ContentForm.jsx`) | Project/content edit revisions; content creation request ID; mounted values only. | Preserve original inputs, context and existing request IDs; safe retries after navigation/lost replies; avoid stale parent/owner/visibility choices. |
| Action/milestone/deliverable controls | Existing create request IDs, edit/transition revisions and mounted dialogs. | Retain recoverable text and request identities, and distinguish reversible field edits from workflow transitions; verify safe undo and current authority. |
| Other record controls and comments | File, team, service, approval, setup and comment forms are inventoried as present; full mutation/recovery paths have not all been inspected. | Review the remaining actual paths before N1 closes. Do not infer that a generic hook makes approval/invitation/generation actions safe to repeat. Email/outreach controls remain paused under the owner's override. |

N1D covers the four existing profile editors only. Sheet undo remains accepted; safe undo across other surfaces needs explicit domain evidence. The whole N1 phase stays open until these applicable requirements are verified.

## N1E — Recover new prospect forms (accepted locally)

Retain the current explicit Create prospect action and labelled identity fields. Use a separate implementation worktree after N1D acceptance. Keep browser copies scoped to their original user/workspace and writer, with the existing seven-day/ten-copy/256KiB bounds. Preserve the original creation request ID across recovery, including incomplete fields, so a lost reply cannot create a second prospect. Show metadata until an explicit read-only server check verifies current prospect-management authority and the bound initiating user/workspace. If the creation receipt already exists for this user, show an authorized link to that prospect and keep changed draft fields available for review/download; do not issue a new creation request automatically.

Canonical creation gains an optional initiating-user check while preserving current callers, the existing creation fingerprint, live administrator guard, source records and activity transaction. Reuse those existing receipts and guards rather than create a parallel prospect store. No new schema is expected. Retain fields through invalid/offline/server failures, block repeated submission, stop stale/disposed writers on scope changes, clear copies on logout and avoid cross-tab copy rewrites. A recovered form never submits automatically and an account/identity prop replacement cannot inherit another user's input. Deliberate discard must preserve another writer's newer copy.

Acceptance includes refresh/navigation/new-context recovery, incomplete field correction, source/receipt fidelity, a real committed-but-lost response before and after reload, edited input after a lost reply, duplicate submission, denied/revoked/foreign/replaced-session access, storage failures/limits and desktop/phone/keyboard checks. Confirm no extra prospect/activity row from retries and no provider request, client conversion, invitation or outreach. Preserve the accepted profile and sheet behavior; compare unrelated bundles/queries. Apply the configured one-reviewer policy. N1E acceptance does not close the remaining N1 inventory.

## N1F — Pages lifecycle and recovery (accepted locally)

Keep the mature RichEditor, Page tree, nested links, sharing, comments and ordinary serial autosave. Cover both internal and portal PageEditor routes, including directly granted and inherited edit permissions. Key the stateful boundary by initiating user, workspace and page. Bind canonical saves to that initiating user without breaking existing callers or weakening current page edit/revision predicates.

Offer recovery metadata before revealing local document contents. An explicit read-only server check must verify the original user/workspace and current edit authority. Recovered input stays unsaved until deliberate confirmation; no restore-triggered autosave. Keep original revision and the exact outstanding save attempt so a committed-but-lost reply can be reconciled without a duplicate save/activity. Edits made during an outstanding request must remain intact. Never silently rebase a conflicting draft over another writer's revision. Retain download and deliberate use-saved actions behind current authority checks.

Use bounded, separate browser copies per user/workspace/page/writer with seven-day expiry and ten-copy limits, checking encoded bytes and reporting quota/oversize failures while retaining mounted input. Preserve current tab's legacy session-storage recovery through an explicit checked recovery path; do not silently discard it during migration. Conditional deletion must protect a newer source copy. Logout clears/invalidate copies across tabs; workspace changes invalidate mounted writers but preserve the original workspace copy. Revoked edit permission, replaced accounts, navigation, page lifecycle events and late responses cannot expose/rewrite old input or resurrect retired copies. No background recovery polling or editor bundles/queries on unrelated routes.

Acceptance: unit and canonical tests for serialized edits, failed/lost replies before/after reload, changed input while pending, conflict resolution, explicit recovery, legacy copy preservation, encoded limits/expiry/CAS, and disposed writers; current direct/inherited/portal edit grants and revoked/read-only/foreign/replaced-user denials. Verify real built-Worker internal/portal editor recovery, keyboard/focus, desktop/phone, saved-state indicators, delayed responses, actual logout/workspace switching and no duplicate save/activity. Preserve content structure, nested page links, sharing and work views. Measure unrelated route load; run the applicable build and one fresh Sol High review. No migration is expected. N1F does not close other forms or the full N1 roadmap.

## N1G — Client creation recovery (accepted locally)

Extend the existing full-page ClientForm and canonical client/contact/activity creation transaction. Preserve Draft/On Track defaults, unlinked primary contacts, owner eligibility and workspace boundaries. No invitation, activation, onboarding generation or prospect conversion side effect. Existing conversion records and history remain unchanged.

Retain incomplete fields and a stable creation request ID in bounded per-writer browser copies, scoped to initiating user/workspace. Use seven-day/ten-copy/256KiB limits, conditional source removal, honest storage warnings and the existing logout/workspace invalidation signal. Reveal only metadata until an explicit bound server check confirms current client.create authority. Never auto-submit recovered input. Key the form by user/workspace; reject late responses and writes after invalidation. Keep original input typed during an outstanding request.

Add an additive durable client-creation receipt with original user, workspace, request ID, normalized-input fingerprint and canonical client reference. The client, primary contact, creation history and receipt commit atomically under live actor and selected-owner eligibility guards. Exact retries return the original authorized client; changed-input reuse reports conflict, never a duplicate or silent edit. Recovery can identify a committed request while preserving later input for download. Preserve compatible non-recovery callers while applying live transaction authority to all creates.

Acceptance: focused schema/domain/store tests; migration from fresh and populated isolated fixtures with idempotency and unchanged old rows; native D1 concurrency/rollback and current role/owner/workspace/user checks; built-Worker incomplete/reload/navigation recovery, real committed-but-lost response, repeated submit, changed input, quota/expiry/CAS, account replacement, logout/workspace switch/revocation, keyboard and desktop/phone. Check canonical record counts and no provider calls. Measure unrelated routes and run the Worker build. One fresh Sol High review with at most one focused re-review after complete implementation. No deployment, live data import, email expansion, paid audit or video work/tests. N1 remains open beyond this slice.

## N1 release checkpoint — current owner scope

Release accepted PRs67–72 together, preserving their existing independent acceptance. No client identity/contact implementation was active when the checkpoint began. Resolve integration conflicts, verify the combined production Worker and relevant non-video tests, prove fresh/second-pass migrations, obtain one fresh Sol High integration/deployment review, merge in dependency order and verify the final main version and affected authenticated flows in staging. Use only synthetic QA records for acceptance. Keep remaining form inventory explicit rather than claiming universal recovery. Record the next N2 client overview task, without implementing it. No production/DNS, real imports/outreach, paid actions or video work.
