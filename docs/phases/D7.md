# D7 — Release D Hardening

Base: verified D6 merge `ccc6e68088cf095f5e0ad9b31603f4ddeca6dd9d`.
Complete Release D under the owner's instruction to continue until input is needed.
No new product features. Do not begin Release E automatically after closure.

## Final acceptance

- Retain mixed GHL/Kajabi adversarial coverage for concurrent generation/request
  isolation, immutable receipt replay, hidden dependency counts and handoff byte
  authorization changing during an R2 await. Current authority must win without
  repairing work, leaking bytes or changing provenance.
- Full suite includes compiler/definition bounds, immutable schema/version/binding
  guards, partial/concurrent generation and late rollback, Project/Service/Client
  scopes, hidden counts, file/Deliverable authorization and approved handoff paths.
- Run disposable native D1 generation for both platforms, setup/binding, Systems
  projection, canonical Home/Work projection and actual workerd D1/R2 file checks.
  Use all committed migrations; measure/retain query and statement limits.
- Build Cloudflare from the committed final candidate, then run the full mixed
  browser story against that stable artifact: setup/selection/retry, QA/rework/
  approval/launch/handoff, canonical portal, current issued-session revocation,
  exact byte downloads, five widths and full-page captures. Synthetic data only.
- One fresh Sol High read-only audit on the exact final candidate commit; at most
  one focused re-review if findings require fixes. Preserve final reviewed head.
- Merge, verify Deploy staging and zero-to-current on the exact resulting SHA,
  check live staging identity and original database inventory restoration. Only
  then close D7 and Release D in the existing status files.

No schema/migration, provider execution, credentials, paid services or production
work. Performance stays closed. Release E requires a new owner direction.

## Candidate acceptance (2026-09-12)

11 new focused / 5,970 full tests and 187 disposable native checks pass. No runtime or migration changes. Full-page browser capture is optional. Final candidate build, mixed browser acceptance, exact-head Sol High audit and post-merge gates remain pending. See BUILD_STATE for evidence and the corrected workspace-wide request-key test expectation.
