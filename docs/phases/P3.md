# P3 — Outreach, Overview and Results

## State

P2 and P3A/P3B/P3C1/P3C2 implementation are accepted locally on `feat/prospecting-p2-eligibility`; see [BUILD_STATE](../BUILD_STATE.md). Dedicated local Google consent and refresh are verified for hello@bloomwired.io. Controlled receipt/send/recovery behavior is locally verified with synthetic provider responses and independently reviewed; one explicitly owner-authorized introduction to aryannelombres@gmail.com was accepted by Google at 2026-09-13T08:06:31.349Z. The owner confirmed receiving it, then requested a clearer sender name and asked about Gmail hiding the signature. Sender name is now Ary at Bloomwired; the separately authorized compact-sign-off test was accepted by Google and received; its sign-off is visible but the owner wants paragraph spacing restored. The subsequently authorized spaced test was accepted once by Google, and the owner replied perfect. Sender presentation is accepted. P3C3A exact-thread observation foundation is locally accepted. P3C3B saved thread checks and durable stop review are implemented and locally verified; independent review is accepted. P3C3D discovery/recovery and mailbox review are accepted locally, and the explicitly owner-approved bounded live mailbox collection is verified:4 messages read,1 matched reply,0 unassigned,catch-up complete; coverage remains unverified and holds remain. Normal preview checks are disabled again. Real outreach and deployment remain paused; full P3, P4 and P5 are unfinished.

## Reuse inventory

- `lib/gmail-send.mjs` has bounded MIME encoding, reply headers and provider send response handling that may be reused if the owner selects Gmail/Google Workspace. Pure helpers must be inspected/tested with the new contract; no existing connection is inherited.
- `lib/gmail.mjs` has Gmail OAuth and metadata/thread primitives. The connection store in `lib/gmail-store.mjs` is inherited slug-keyed storage. The existing `app/api/gmail/*` routes use `getWorkspace`, which deliberately denies fresh Prospecting workspaces through `legacyAppAllowed`. Do not relax that boundary or route the new workspace into the legacy send engine.
- `lib/followup-schedule.mjs` imports old priority/cutover rules and includes a fourth-email step. It cannot define P3's three-message/business-day schedule. Retain the old implementation for its original workspace; implement the approved new schedule separately with timezone/DST fixtures.
- Canonical P1/P2 profiles and immutable manual skill results already supply reviewed fields, intro drafts, two follow-up suggestions and uncertainty. A saved result does not approve sending or establish a provider-confirmed message/reply.

## Next bounded task

The corrected built UI has verified the owner’s reply in the original test conversation and persisted a checked/held state. P3C3B live acceptance is complete; the owner’s standing direction authorizes necessary checks in this existing test context without repeated permission questions. Approved **Ary at Bloomwired** sender name and paragraph spacing remain preserved. No real outreach or deployment is enabled.

P3C3C recipient protection across duplicate prospects is implemented and locally verified, with independent Sol High review accepted. P3C3D1 durable verified provider-returned identity is now locally verified and independently reviewed. P3C3D2 bounded metadata-only discovery against registered identities is now locally verified and independently reviewed. P3C3D3 durable progress/holds and D4 recovery collection/review now pass local acceptance and a separately approved live check. P3C3E1 fields, P3C3E2 MIME association, P3C3E3 exact-candidate provider retrieval and P3C3E4 guarded durable report evidence are accepted locally. Next is the current-account report review surface. Address-only/subject-only matching remains forbidden. Original-submitted-ID-only recovery can remain unresolved when Google returns a different RFC ID; never resend to resolve ambiguity. BUILD_STATE owns current evidence and next scope.

## Later P3 acceptance boundaries

- Three cold messages total: initial, three recipient-local business days later, then five further business days later, with weekdays 9–11 a.m. and explicit timezone/DST handling.
- Real provider message/thread references, stop checks immediately before send, retry/idempotency and ambiguous-response reconciliation.
- Any reply pauses; opt-out, decline and hard bounce stop. Auto-replies do not become human positive outcomes. Approval edits/revocation and reply/send races must fail safely.
- Distinct contacted people versus provider-accepted messages, visible cohort/numerator/denominator/as-of dates, and unknown distinct from zero. No legacy history blending.
- Provider connection/consent, controlled delivery and deployment remain separate explicit external actions. Real outreach and video work/tests remain paused. No production/DNS change.

P3A's schema/API contract follows the confirmed provider/address answer below. Astra High is appropriate; authorization/persistence/provider boundaries require the configured fresh Sol High review and at most one focused re-review when implemented. This preparation itself uses self-review and repository evidence only.

## Current slice: P3A sender setup, draft review and Overview

- Continue in the existing isolated P2/P3 worktree, preserving all accepted uncommitted work and original worktrees. Preflight main remains 651d9a5 and PR #60 head 67b94eb. Record the confirmed provider/address as owner direction, not a global default for every workspace. The form starts from that workspace's own settings; the authorized local fixture uses hello@bloomwired.io without contacting it.
- Add one workspace-scoped Google Workspace sender record with email, optional display name, revision and actor/time. Saving identity never connects Google, copies legacy credentials or enables sending. Current Owner/Admin Prospecting authority and exact revision guard every read/write. Invalid/stale/revoked requests create no partial events.
- Add one current three-message draft per prospect: recipient, timezone, intro subject/body and two follow-up bodies, optional saved-result provenance, source-profile revision, revision and actor/time. The form can explicitly start from current canonical intro plus a selected saved result's follow-up suggestions; user edits are visible and never automatically applied from later results. No saved result is chosen implicitly. Drafts can be incomplete.
- Approval is a separate explicit action against current draft, sender and profile revisions. Require complete messages, a valid timezone, sender identity, Strong fit, checked current public recipient and dated evidence/proposed work, plus a human review acknowledgement. Persist an immutable approval snapshot and one activity event. Approval is content review, not a send. Edited draft/profile/sender or a revoked/demoted approving member invalidates current approval while retaining its history. Retries cannot duplicate approvals or overwrite later edits.
- Overview lists only explicitly saved drafts and their actual review state, with clear grouped decisions and sender setup. Untouched inventory is not an urgent queue. Sending remains honestly off with no connection/provider/send counters. Preserve existing Prospect list, skill navigation, Bloomsi logo and favicon/garden profile identity. Full-page labelled forms, meaningful compact icons, comfortable five-width layout; no metadata chains.
- Atomic D1 batches and additive relational migrations only. Approval snapshots are immutable provenance; current fields/state remain relational. No provider request, callback, secret, paid dependency, real import/outreach, commit/push/deploy, production/DNS or video work/tests.
- Acceptance: focused SQL-backed authority/isolation, valid/invalid inputs, incomplete draft/approval gates, edits/revocations, retries/concurrency and atomic rollback tests; populated/fresh/repeat migrations with prior-row preservation; final Worker build; native/browser sender/draft/save/review/Overview/conflict/error/keyboard/five-width flows. One fresh Sol High review and at most one focused re-review.


## P3A verification and next boundary

P3A's three-message draft, sender identity, content approval and saved-draft Overview pass 78 distinct focused tests, the final Worker build, 58 final native/browser checks and populated/fresh/repeat migration proof. Migration `0028` reaches 29 domain migrations/54 tables. All pre-slice rows across 87 tables are preserved. [Visual preview](../previews/prospecting-p3a/README.md). BUILD_STATE records independent review acceptance separately.

Full P3 remains unfinished. Next define P3B: new-workspace Google connection/consent and exact recipient-local schedule preview, with explicit authority, account/address verification and isolation from legacy connections. No real delivery, connection or secret change is authorized by the P3A implementation. Confirm configured callback/client requirements through repository evidence before requesting credentials or owner action. Actual sending needs its own durable provider/retry/stop contract and controlled acceptance; Results remains blocked on true provider events. Continue on Astra High for those security and cross-system boundaries.


## Current slice: P3B connection and schedule preview

Preflight: main 651d9a5 and PR #60 head 67b94eb unchanged; continue the same isolated worktree preserving accepted P3A. Dedicated Google OAuth client ID/secret and token-encryption key are absent locally. Implement/test with synthetic credentials and injected provider responses; do not configure real credentials or contact Google during verification.

- Dedicated `BLOOMOPS_GOOGLE_CLIENT_ID`, `BLOOMOPS_GOOGLE_CLIENT_SECRET`, `BLOOMOPS_GOOGLE_TOKEN_KEY` (32 random bytes encoded as 64 hex characters) and explicit `BLOOMOPS_GOOGLE_CONNECT_ENABLED=true` gate connection. Use configured `BLOOMOPS_APP_URL` for the exact new callback; never derive it from untrusted request headers or use legacy client/key fallbacks. Google setup remains inactive when configuration is missing.
- New-workspace Owner/Admin may explicitly begin OAuth for the saved sender. Random single-use state is stored hashed, bound to Better Auth session, user/membership update stamp, workspace, sender revision and connection revision, with a 10-minute expiry and PKCE. Consume it atomically before exchanging the code. Require live authority again before storing the result. Cross-user/workspace/session, stale sender, revoked member, replay, denied/partial consent and concurrent callbacks cannot attach or overwrite a connection.
- Use Google web-server authorization with Gmail readonly and send scopes, offline consent, and fixed Google endpoints. Readonly supports mailbox identity, send-as aliases and later reply inspection; no mailbox contents are read now. Validate the granted scopes and refresh token, current primary account and accepted matching send-as address. This supports hello@bloomwired.io whether it is primary or an accepted alias. The client only receives safe status/address fields; codes, states and tokens never enter logs or rendered pages.
- New relational OAuth-attempt and connection tables have same-workspace/actor foreign keys. Encrypt token/verifier data with AES-GCM and workspace/purpose-bound associated data under the dedicated key; no plaintext fallback. No legacy connection/key/table changes. Connection status is valid only for the current sender address and a still-active authorizing Owner/Admin membership/update stamp. An explicit local disconnect clears the stored grant and invalidates outstanding callbacks; it does not revoke other Google grants or delete historical activity.
- Connect/disconnect does not enable sending, create jobs, change content approvals or count as an email. Overview and sender setup display the actual connection state but keep sending off. A failed/replayed callback redirects to a fixed safe sender page with a generic outcome, never raw provider text or a code/state in the redirect.
- Read-only schedule preview uses the current saved draft timezone and approval/profile/sender revision guards. An explicit earliest UTC instant produces the next recipient-local weekday window (9:00 inclusive–11:00 exclusive), then three business days later and five further business days later, at the same local clock time. Business days mean Monday–Friday only; holidays are not inferred. DST uses IANA/Intl, with explicit failure for invalid/ambiguous/nonexistent local times. Preview is not persistence, scheduling, approval or sending.
- Verify SQL-backed auth/expiry/session/replay/revision/race/atomic rollback, cryptographic separation, provider errors/partial consent/aliases and no secrets in DTOs. Test schedule weekend/month/year/DST and fractional offsets, body/origin checks, native D1 flow with injected provider, five-width UI/keyboard/errors, final Worker build, additive populated/fresh/repeat migrations and original-row preservation. One fresh Sol High review and at most one focused re-review.
- No real OAuth grant, remote secret, provider send/reply/watch, paid dependency, import, commit/push/deploy, production/DNS or video work/tests. Live Google consent remains an external acceptance gate after the code is reviewable.

Google reference: [web-server OAuth](https://developers.google.com/identity/protocols/oauth2/web-server), [send-as listing scopes](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.settings.sendAs/list), [Gmail scopes](https://developers.google.com/workspace/gmail/api/auth/scopes). Implementation uses built-in Web Crypto/fetch and the existing D1/Drizzle stack; no added SDK is needed for these fixed HTTP calls.


## P3B local acceptance and external gate

P3B is implemented, locally verified and independently reviewed. Final evidence: 81 distinct focused tests, final Google 18/18, native injected-provider 13/13, built-browser/native-HTTP 48/48 at five widths, final Worker build, additive `0029` populated/fresh/repeat migration proof. This branch has 30 domain migrations and 56 domain tables; prior application rows and foreign keys are preserved. [Visual preview](../previews/prospecting-p3b/README.md).

One Sol High review and the single focused re-review resolved primary-address handling: Google verificationStatus applies to custom aliases; an isPrimary row instead requires a matching authenticated mailbox profile. No material review finding remains. The real local consent/account-verification path now passes for the owner-selected sender. Subsequent owner-authorized setup installed and reviewed dedicated local credentials; the selected local Owner is signed in and initial owner-approved callbacks exposed an unsupported Worker redirect option. The verified local fix uses manual redirects with non-success rejection; fresh owner consent then successfully connected `hello@bloomwired.io` in the separate test workspace, with encrypted tokens and sending off. [Google setup](../GOOGLE_CONNECTION_SETUP.md) makes the pending project/client/callback steps reviewable. Project `bloomsi`, Gmail API enablement, Internal audience and the local-only callback are verified; see BUILD_STATE for current setup evidence. No secret in chat; no original-LTB connection reuse.

Local work and the real consent/account-verification path are accepted, not full P3 completion. The dedicated local grant is connected; no commit/push/deploy, mail delivery or video work/tests occurred. Next is the Google token refresh/revocation and remaining controlled connection-lifecycle contract, then the separate P3 send/retry/stop/reply contract. Results and real outreach remain unimplemented.

## Current slice: P3C1 connection checks and token refresh

Continue the isolated P2/P3 worktree on main651d9a5/PR60head67b94eb preflight, preserving accepted work and the real local grant. Existing consent authorizes local refresh/profile/send-as checks; do not revoke that grant or read message contents. No delivery, watch, automatic background check, cron, queue, import, deployment or video work.

- Add an explicit Owner/Admin **Check connection** action on Sender setup and a bounded same-origin POST `/api/bloomops/prospecting/google/check`. GET status remains provider-free. Request includes workspace, sender and connection revisions. Check always exchanges the current refresh token, then verifies the same primary Google account and saved primary/accepted sender address. Both required scopes must remain granted. Refresh responses may omit a new refresh token/scope; retain the previously verified values, but reject malformed or reduced values when provided.
- Keep tokens encrypted and server-only. No response or log includes tokens, provider descriptions or arbitrary errors. All provider requests remain fixed-endpoint, bounded/time-limited and reject redirects. A confirmed invalid refresh grant, reduced consent, changed account or no-longer-authorized sender requires reconnection; transport, configuration, rate-limit and server failures remain retryable without clearing the existing encrypted grant. No provider revoke endpoint is called.
- Add nullable lease/check timestamps and a bounded last-check status to the existing connection. Claim a 90-second check lease with exact revision/current session/current actor/current original-authorizer/current sender checks and increment the connection revision. Concurrent/stale requests cannot contact Google twice. A new check may recover an expired lease; requests within30seconds of a completed check are held. Completion requires the same lease/revision, unexpired lease, sender revision, actor/session and original-authorizer stamps. Disconnect/reconnect or authority/sender edits win over a late check. Completion and its scoped activity event are atomic.
- Keep disconnected/reconnect/temporary/expired/checking states distinct. Expired access never appears currently verified. Show the last successful/attempted check where useful and a concise retry/reconnect message, preserving the existing Bloomsi layout and spacing. Sending stays off. No new token consumer or delivery permission is introduced.
- Acceptance: focused SQL-backed denied-role/workspace/session/revision tests; rotation/omitted-token handling, expiry, invalid-grant versus transient failures, redirects, malformed data, race/lease recovery and rollback tests; real workerd global-fetch coverage with synthetic outbound responses; populated/fresh/repeat additive migration proof and prior-row preservation; final Worker build; browser check/error/retry/keyboard/five-width flows and one controlled live check of the already-connected owner grant after review. One fresh Sol High review and at most one focused re-review.

Google's [offline access and refresh contract](https://developers.google.com/identity/protocols/oauth2/web-server#offline) defines token refresh without another consent prompt. This slice detects revoked/invalid access only when explicitly checked; it does not claim continuous monitoring. Later delivery must consume a separately guarded fresh grant and implement durable stop/retry/reply handling before controlled sending.

## P3C1 acceptance

Implemented, locally verified and independently reviewed:78 focused tests, native workerd consent/refresh success and30redirect cases,15 native D1 checks,36 browser/native HTTP checks at five widths, final Worker build, additive0030 populated/fresh/repeat proof. One fresh Sol High review found no material issue. The controlled live refresh succeeds for the existing owner grant with renewed access, encrypted storage, released lease and one check event; sending remains off. [Preview](../previews/prospecting-p3c1/README.md). BUILD_STATE owns exact evidence and outstanding P3 boundaries. Next: controlled delivery preparation; actual email requires a specified test recipient and explicit authorization.


## Current slice: P3C2 controlled introduction delivery

P3C1 connection reliability is accepted locally. Continue in the same isolated worktree. This slice prepares and verifies a local-only, single-introduction test; it does not authorize a real email. Preserve the live grant and its key.

- Prepare a durable receipt from one current immutable content approval, with a stable RFC Message-ID. One receipt per workspace/prospect; preparation and cancellation never contact Google. Identity/content references cannot be rewritten or deleted. An edited or revoked approval blocks delivery.
- Explicit send requires current Owner/Admin authority and live session, the exact prepared receipt, human acknowledgement, healthy unexpired dedicated Google access, and local development configuration naming that one receipt and exact recipient. No recipient override, legacy connection, background job, automatic follow-up or deployed send path. The controlled test is immediate and explicitly outside cold-outreach scheduling.
- Claim once atomically before the provider request, checking current profile/draft/sender/approval/grant and member stamps. Cancellation wins before claim. A final check before transport catches intervening changes; already submitted mail cannot be recalled. Durable provider outcomes are retained even if authority is revoked while the request is in flight; response reads still enforce current authority.
- Every attempted send stays permanently protected from resubmission. Transport errors, non-success and malformed responses are unresolved, never a reason to resend. An explicit reconciliation searches only Sent for the receipt's exact RFC Message-ID, then verifies matching message metadata/address/subject; no match or ambiguity stays unresolved. Google acceptance is distinct from inbox delivery.
- UI uses an existing full-page review with labelled sender/recipient/message, compact icons, clear prepared/cancelled/unresolved/accepted states and explicit send acknowledgement. No dot-separated metadata chains. Show disabled delivery honestly; preserve existing profile identity and draft workflow.
- Verify SQL/native D1 authority, tenant isolation, exact config gates, stale/revoked states, concurrent preparation/send/cancel, interrupted and ambiguous sends, response reconciliation, event rollback, encrypted-token privacy, bounded provider requests, MIME/injection protection and preserved prior rows. Run additive populated/fresh/repeat migration proof, final Worker build and five-width browser/error/keyboard flows with synthetic intercepted provider traffic only. One fresh read-only Sol High review, at most one focused re-review.
- Before a real test, present its exact message and ask for the owner-controlled recipient and explicit single-email authorization. No real send, mailbox read, deployment, remote change, import, production/DNS or video test in local acceptance. Remaining P3 includes reply/stop ingestion, scheduled sequence recovery and Results.

Provider references: [send](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/send), [exact-message search](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/list) and [message metadata](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/get).

## Current slice: P3C3A exact-thread observation foundation

Read-only preflight confirms main651d9a5 and PR60head67b94eb unchanged. Keep the same isolated worktree and accepted sender format. P3C3 is split at the provider/domain boundary so untrusted mailbox observations are verified before a route can persist them or influence sending.

- Implement a server-side reader for metadata of exactly one accepted receipt's original Google thread. Caller supplies the receipt, immutable sender/recipient snapshot and verified original Google account; validate accepted status, provider IDs and account match before transport. No address/domain/subject search, mailbox list/history/watch, attachment/body download, legacy grant lookup or sending. No application route calls this foundation yet; token/session/workspace authorization remains mandatory in the next integration slice.
- Verify the thread includes the exact original SENT provider message with its RFC Message-ID and reviewed sender/recipient. Match replies by an explicit In-Reply-To/References path to that anchor, never subject similarity or address/domain fallback. Unknown participants with valid ancestry may reply; missing/ambiguous ancestry is held for review, not attributed as a human reply. Ignore actual own SENT messages and drafts. An unlabelled apparent self-message cannot establish a human reply.
- Metadata yields unreviewed reply, automatic response, delivery report or needs-review observations. All inbound/ambiguous observations request a hold. Empty observations mean only no observation in this checked thread, never permission to send. Missing Auto-Submitted does not establish a human or positive outcome; no response body is classified. Delivery reports require later DSN validation/manual evidence before being called hard bounces. No automatic resume.
- Bound transport to a fixed Google GET endpoint,15-second deadline,256KiB body,100 messages and bounded unique headers/IDs. Redirects, errors, malformed/truncated/oversized responses, duplicate provider/RFC IDs or missing sent anchor return an unresolved hold with no partial observations or raw errors. Stable provider IDs support later deduplication. No tokens, snippets, raw headers, body content or provider diagnostics in results.
- Add focused synthetic parsing/provider tests and real workerd global-fetch verification. Confirm existing local DB/credentials and accepted source files remain unchanged. No UI/schema/migration change means no browser or migration acceptance is needed for this standalone module. One fresh Sol High read-only review and at most one focused re-review, per policy.

### P3C3 remaining integration contract

Next P3C3B adds explicit Owner/Admin exact-thread checking with live session/membership/original-account/grant revision checks before transport and persistence; a lease/cooldown and additive same-workspace relational check/observation/stop records; atomic deduplication/activity; monotonic pause/stop state and manual opt-out/decline/hard-bounce decisions with provenance. Current authority and account edits win over late checks. A later empty check cannot erase an earlier reply/stop. Distinguish unknown, checked, held and stopped in the full-page review/Overview. Do not mark sender-provided metadata as confirmed human/positive Results.

Before any scheduled sequence is enabled, finish opt-out/decline classification or explicit review, verified DSN hard-bounce ingestion including reports outside the original thread, broader reply discovery with strict tenant/account/receipt matching and gap recovery, and atomic stop checks at claim and immediately before transport. Sending already submitted cannot be recalled. No scheduled/follow-up sending is introduced by P3C3A. Live mailbox acceptance requires a separate exact-thread authorization after the integrated path is concrete and verified; existing test-send authorizations are consumed.

References: [Gmail thread metadata](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.threads/get), [message metadata and internalDate](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages), [automatic responses](https://www.rfc-editor.org/rfc/rfc3834.html), [delivery-status reports](https://www.rfc-editor.org/rfc/rfc3464.html), [Gmail SENT system label](https://developers.google.com/workspace/gmail/api/guides/labels).

### P3C3A local verification

The standalone exact-thread reader passes43 focused tests plus one native workerd test exercising18 intercepted provider requests, including a stalled response body's15-second timeout. Syntax/hygiene and unchanged prior-source/92-table/credential checks pass. No production app route imports the new module, so it cannot read the live connected mailbox. One fresh Sol High review found an alternate-alias false hold; corrected the reader to ignore Gmail's per-message SENT mail and added a reproducing regression. The single focused Sol High re-review accepts the correction with no remaining finding; BUILD_STATE records final acceptance. P3C3B implementation and verification follow below; this paragraph records the earlier standalone acceptance.

## Current slice: P3C3B saved thread checks and stop review

- Add an explicit full-page Reply and stop review linked from an accepted delivery. GET reads local records only. Owner/Admin can check exactly that accepted receipt's original thread after an acknowledgement; a development/loopback/r2-dev flag names that one receipt. No live mailbox access during implementation, and no send path is enabled. Use the accepted P3C3A reader, original immutable approval/account references and healthy dedicated Google grant.
- Both API commands require current workspace/session/Owner/Admin authority and exact record revision. Check additionally requires current sender and connection revisions, matching original account/sender, current original authorizer, valid encrypted token/scopes and time margin. Recheck before transport and before persistence; late account/sender/member/session changes cannot attach observations. A90-second lease and30-second completion cooldown prevent duplicate provider calls. Expired checks can recover without clearing prior holds.
- Add relational state per accepted receipt/prospect and immutable deduplicated provider observations with same-workspace/receipt/member foreign keys. State/lease changes, observations and activity commit atomically. A provider response is untrusted until the exact thread anchor is verified. Provider message identity is unique per workspace/account and cannot move between prospects. No tokens, bodies or raw headers are persisted. Show recent observations and clear checked/unresolved/checking states without claiming full mailbox coverage.
- Any observed inbound or unresolved check sets a durable hold. A later empty successful check never clears it. Explicit human Stop outreach requires an opt-out, decline, confirmed hard-bounce or manual reason and short evidence note. It works without Google access, atomically supersedes an in-flight check, records human provenance and cannot be silently resumed or rewritten. It is not a provider-confirmed classification or Results metric.
- Stop/hold applies to the recorded prospect and original recipient context; address-wide suppression across duplicate prospects, broader reply discovery, validated DSNs outside the original thread and final scheduled-send guards remain later prerequisites before real outreach. P3C2 already forbids any resend/follow-up for accepted receipts. No new sequence, cron, queue, body classification, automatic resume or reply sending.
- Verify SQL authority/revision/lease/race/rollback/deduplication/monotonic state and migration constraints; populated/fresh/repeat migration and prior-row preservation; native D1; built-browser API/keyboard/error/five-width layout; Worker build; one fresh Sol High review and at most one focused re-review. Preserve approved logo/profile icons and spaced email format. No real mailbox read/send, credential/remote/deployment change or video work/tests. After local acceptance, request only the owner's exact-thread test reply/reading authorization needed for live acceptance.

### P3C3B local acceptance

Saved exact-thread checks, deduplicated observations, durable holds, permanent human stops and Overview conversations are locally accepted. Migration0032 reaches33 domain migrations/59 tables; populated/fresh/repeat proof and preservation of92 prior application tables/credentials pass. Verification:100 distinct focused tests (23 final reply-domain plus77 unchanged schema/delivery/provider cases),16 native D1 checks,41 final built-browser checks at five widths and final Worker build. [Visual preview](../previews/prospecting-p3c3b/README.md).

One fresh Sol High review found the per-observation collision guard exceeded D1's100-parameter limit on long threads. Binding one JSON array through `json_each` corrects it without weakening collision/atomicity checks. Both64/99-observation regressions reproduce the old failure, then pass; native D1 saves an actual100-message thread. The single focused Sol High re-review accepts the fix with no remaining finding. No further review loop is needed.

Normal local preview sends/checks are disabled. No live mailbox read or new email occurred. The owner’s reply and separately bounded one-thread authorization are the next acceptance gate; full P3 and the later scheduling prerequisites remain unfinished. BUILD_STATE owns current handoff details.

### P3C3B first live check

Owner-authorized single check completed09:27:55.176Z as unresolved and held, with zero verified observations. This is not an absent-reply claim. Normal preview restored with send/check disabled. The refreshed connection, one held state and3 activity events are the only changes across94 application tables; all prior other records/credentials preserved. The live gate remains unresolved. A private one-use sanitized diagnostic helper is prepared and synthetic-tested, not executed. BUILD_STATE records the accepted helper review and one additional exact-thread authorization needed for diagnosis.

### P3C3B accepted-provider anchor compatibility follow-up

The owner authorized one additional header-only diagnostic check with **yes go**. It returned HTTP200, the exact original thread with two messages and the accepted SENT provider message. Its returned RFC Message-ID differs from the submitted receipt identity; this explains the prior equality rejection. The diagnostic contains only status/count/validation flags, not header values or content. The check remained unresolved/held. Both live-read authorizations are consumed; no further mailbox read is authorized.

For this local correction, keep immutable submitted receipt provenance and exact original-account/provider-thread/provider-message/SENT/sender/recipient checks. Require a valid unique RFC Message-ID on that exact accepted provider message and use its returned identity as the ancestry root. A submitted RFC ID by itself is never a fallback. Never choose another sent message, subject/address/domain match, malformed/missing header or draft as the anchor. All inbound/unresolved outcomes remain held; no automatic resume or positive/confirmed-human classification.

Verify both matching returned-ID and unchanged-ID cases, descendants, no submitted-ID fallback, missing/invalid/duplicate identities, original provider/account/SENT/address denials and immutable receipt preservation. Run affected provider/domain tests, real workerd metadata/timeout checks, native D1 and final built-browser checks with synthetic returned-ID fixtures. No schema/migration/credential change. One fresh Sol High review, at most one focused re-review. After local acceptance, a further live check of the original thread requires separate owner authorization; no new email/reply is needed.

## Current slice: P3C3C recipient protection across duplicate prospects

P3C3B live acceptance now passes: the corrected built UI linked one incoming reply to the original accepted conversation and saved a checked/held state. The owner's standing approval covers further necessary checks of this existing test conversation without asking per attempt. Preserve that scope; no real outreach or production deployment is activated.

- Reuse existing immutable accepted receipts/approval snapshots and durable reply states as the source of recipient protection; do not create a second suppression ledger. Within the current workspace, an existing stopped/held or unresolved/in-flight check blocks a new receipt and introduction to its original recipient, including through another prospect. No migration is needed for this derived guard. Never use the source prospect's mutable current email in place of its accepted recipient.
- Match the exact normalized email used by existing prospect/outreach validation. Do not infer aliases, remove Gmail dots/plus suffixes, merge workspaces or share connection/history. Protection applies across sender changes within the same workspace. A confirmed empty check with no prior hold does not create a block; a pending/expired unresolved check stays blocking until reviewed/recovered. Existing holds/stops remain monotonic.
- Apply the common guard to preparation, the atomic send claim and the final pretransport check. A newly observed hold/stop or pending check wins before claim or final transport; late events after submission cannot recall mail and must not erase the actual provider outcome. Existing accepted receipts and uncertain receipt reconciliation remain readable and cannot resend. Content drafting/approval may continue but never authorizes delivery past protection.
- Owner/Admin delivery review shows a readable stopped/held/check-pending explanation for the target address and a link to its source conversation; preparation/send controls reflect the server gate. No raw stop evidence/body/token leakage, metadata chains or new settings. Preserve navigation, logo and garden identity.
- Verify exact/canonical/alias boundaries, same/foreign workspace and denied roles, mutable source-email edits, disconnected/sender changes, read-only behavior, stale client attempts, before-claim/pretransport/in-flight races and atomic event behavior. Exercise native D1 and built browser desktop/mobile/keyboard/error flows with synthetic intercepted provider traffic. Reuse existing records/migrations; prove main DB/credentials unchanged after this local implementation. One fresh Sol High review, at most one focused re-review. No live provider call/send or deployment for this slice.

Later P3 still needs broader reply/DSN discovery and recovery, final safeguards in any future scheduler, sequence execution and truthful Results. This guard does not claim discovery coverage or infer an opt-out from an ordinary reply.

### P3C3C local acceptance

Recipient protection across duplicate prospects passes52 distinct focused tests,20 native D1 checks,51 built-browser checks at five widths and the final Worker build. One fresh Sol High reviewer found no material issue and independently reran13/13 protection tests; no re-review was needed. The stopped UI branch was directly exercised in the browser; held/checking wording was inspected through code and domain tests. All94 application tables and credential bytes unchanged by implementation; no migration/schema/dependency change. Normal preview is current with send/check disabled. [Visual preview](../previews/prospecting-p3c3c/README.md). P3C3B live acceptance is complete; full P3 remains unfinished.

### Next bounded contract: P3C3D discovery/recovery foundation

Start with verified provider identity provenance before broader mailbox discovery. The returned RFC identity of an exactly accepted SENT provider message can differ from the submitted Message-ID; broader reply/DSN matching therefore needs a durable, immutable, same-workspace/account/delivery association. Preserve submitted receipt provenance and require the reviewed original account/thread/provider-message/SENT/address gates before recording any returned identity. Duplicate/replayed observations must not create duplicate records/events; conflicting identities stay unresolved and held. Empty checks cannot grant sending permission.

Inspect reuse in `prospect-reply-observations.mjs`, `prospect-replies.mjs`, `prospect-delivery-provider.mjs` and the existing relational schema before choosing the additive migration. Subsequent discovery must match registered delivery identities, handle mailbox gaps/replays conservatively and validate delivery-status evidence before calling it a hard bounce. No subject/address/domain-only attribution. Original-ID-only uncertain-send recovery stays unresolved when no verified match exists; do not resend. Define focused provider/domain/native/migration acceptance before implementation. Keep scheduler, follow-up sends, real prospect imports, production and video work/tests off. The owner's standing direction avoids repeated routine test-conversation approvals; do not reinterpret historical one-check gates as current blockers.

### P3C3D1 acceptance: verified provider identity registry

Implement the first P3C3D foundation as one additive immutable relational table, linked to the original accepted delivery, workspace/account and verifying membership, with sender/connection revision and verification time. Register only the valid unique returned RFC identity from the existing exact SENT provider-message/thread/address gates. Keep the submitted receipt unchanged. The actual identity remains server-only; activity exposes only delivery association, never raw headers or credentials.

Persist the association, a single creation event, observations and completed state in the existing atomic completion batch. Replay reuses the association without duplicate creation events. Conflicting identities (including a competing delivery in the same workspace/account) save no new association or observations and finish unresolved/held. Re-evaluate collision and authority conditions inside the batch; manual stop, revoked authority and expired/replaced leases win. Empty checks may verify an identity but never clear a previous hold or grant sending permission. No live backfill, new endpoint, mailbox search, DSN classification or uncertain-send retry.

Acceptance: unchanged/different submitted RFC IDs; replay and identity conflicts at preflight/completion; provider/account/workspace boundaries; malformed/missing anchors; authority/stop/lease races; rollback and at most100 D1 parameters for100-message threads; immutable schema/account/receipt/member constraints and safe DTO/activity. Run focused provider/domain/schema tests, real workerd reader acceptance, native D1 integration, fresh/repeated and populated additive migration proof, final Worker build and existing built-browser flow with synthetic provider interception. Preserve original local data/credentials and restore disabled normal preview. One fresh Sol High read-only review after acceptance, at most one focused re-review.

### P3C3D1 local acceptance

Implemented and independently reviewed:121 distinct focused tests,24 native D1 checks,51 built-browser checks and final Worker build pass. Migration0033 passes fresh/repeat/populated verification and is applied locally; all94 prior application tables and credentials are preserved. Main identity registry remains empty without live backfill. Exact accepted SENT metadata is the only registration source; replays preserve provenance and conflicts finish unresolved/held. One Sol High review found no material issue and independently reran17 identity tests; no re-review needed. Normal8787 preview is restored with checks/sending disabled, isolated8788 stopped. BUILD_STATE owns evidence and next scope.

### Next bounded task: P3C3D2 discovery provider foundation

Inspect reuse from existing `gmail-sync.mjs`, `gmail-thread.mjs`, `reply-match.mjs` and ingestion helpers. Keep their legacy workspaces, cursors and connections untouched. Define focused acceptance before implementation for bounded metadata-only candidate discovery and exact ancestry matching against registered same-workspace/account accepted delivery identities. No subject/address/domain fallback. Missing identity, ambiguous ancestry, mailbox gaps and incomplete pages remain unresolved; no empty-page or cursor assumption grants sending. Do not add background monitoring, live broad-mailbox access, follow-up sending or hard-bounce classification in this provider foundation. DSN evidence and durable gap/replay recovery need their own bounded acceptance.

### P3C3D2 acceptance: bounded history/metadata provider reader

Implement a provider-only reader, with no app route, database mutation or active mailbox monitoring. Inputs must come from a future authorized caller: one workspace/account, stored start history ID and at most100 registered identity/accepted-receipt/approval-snapshot associations. Validate association workspace/prospect/delivery/account/provider/RFC consistency before transport. Reuse strict P3 reply metadata parsing and transport bounds; keep legacy modules unchanged because their address/domain fallback, body reads, queue writes and expired-cursor reset violate this contract.

Read Gmail message-added history from the supplied cursor, without an Inbox label filter, up to3 pages and40 unique messages. Request only IDs/history pagination and the existing9 approved message headers. Use one15-second total deadline,256KiB per history page/64KiB per metadata response and1MiB total bytes. Never retry, follow redirects, read bodies/snippets or bootstrap/reset from the current mailbox profile. Compare history IDs numerically, deduplicate repeated message additions, validate pagination/order/message identity, and offer a next cursor only after complete terminal history and metadata processing. Any expired cursor, incomplete pagination, missing/deleted/malformed message, budget overflow or inconsistent response keeps next cursor null.

Match direct or transitive RFC ancestry to exactly one registered accepted delivery, across provider threads and response order. Verify any registered original message present in the window against its exact SENT/thread/address identity; never infer another root. Conflicting roots, duplicate RFC IDs, malformed/missing ancestry and unattributed inbound messages keep the entire batch unresolved with no proposed observations/cursor. This intentionally requires later durable unattributed/gap handling before unattended use. Exclude drafts and own SENT from incoming observations; automatic responses and delivery reports remain distinct unreviewed holds, never confirmed human/positive/hard bounce. Empty success is a bounded snapshot with a conservative hold, never send permission. Return no raw headers, sender/recipient addresses, unrelated message IDs, bodies or tokens.

Verify pure matching, exact ancestry and ambiguity, tenant/account/registry validation, numeric history ordering beyond JS safe integers, paginated duplicates/replays, incomplete/gap failures, exact GET/header/field limits, body cancellation and shared deadline using focused Node and real workerd synthetic interception tests. Run existing provider/workerd and affected reply/identity regression tests, final Worker build and source/data/credential preservation checks. No schema or UI change: native migrations/browser visuals are not new acceptance requirements for this provider-only slice. One fresh Sol High read-only review after acceptance, at most one focused re-review.

Provider contract references inspected: [Google history list](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.history/list) (terminal pagination, numeric history and expired404 behavior) and [message metadata](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/get).

### P3C3D2 local acceptance

Provider-only discovery passes128 distinct focused tests (127 aggregate before review,38 affected discovery tests after the correction), both real workerd readers, final Worker build and preservation/hygiene. Sol High found an unsupported100-additions-per-record limit;101 duplicate additions now deduplicate within the existing byte and unique-message limits. Regression fails before the fix and passes after it. The single focused re-review resolves the issue, independently passing38/38 affected tests. No remaining material finding. All95 application tables/schema/credentials/unrelated source are preserved, no migration or UI change, no app route/job or live mailbox access enabled. Normal preview restored with sending/checks disabled. BUILD_STATE owns exact evidence.

### Next bounded task: P3C3D3 durable discovery/recovery state

Define the same-workspace/account stored cursor, lease and immutable identity-snapshot requirements before implementation. Reuse existing session/member/grant revision and atomic activity patterns. A cursor advances only with successfully persisted complete evidence; interruptions, revocation, stops and replay cannot skip mail or authorize a send. Expired/incomplete/unattributed batches need durable conservative recovery holds. Define bootstrap/backfill coverage explicitly: do not jump to the current mailbox profile and imply earlier accepted conversations were checked. Keep new broad live mailbox access, scheduled/follow-up sending, confirmed DSN classifications, real imports/outreach and production off.

### P3C3D3 acceptance: guarded saved discovery runs

Implement a local-only domain command with explicit workspace/account enablement, no HTTP route or job. Reuse current Owner/Admin/session/member/grant/sender checks. Store one versioned state per workspace/account, immutable run provenance with one terminal result, and relational immutable run-target rows tying accepted receipts, registered identities and reply-state revisions to the run. Additive migration only.

Claim with revision,90-second lease and30-second completed-run cooldown. Atomically hold every accepted recipient in the bounded account set before provider access, preserving prior held/stopped evidence and reusing existing reply-state/recipient protection. Snapshot the full1..100 accepted-delivery set; incomplete registry finishes unresolved without provider access. Refuse oversized sets and any outstanding exact-thread lease before claiming, including expired leases that need their own exact-thread recovery. Current target/identity/reply revisions, membership/session, original authorizer, sender and grant must match immediately before transport and completion. New deliveries/identities, manual stops, competing checks, revocation and replaced/expired leases cannot advance a cursor. Reclaim an expired run by finalizing it superseded, never accepting its late result.

First startup may record a verified account profile history baseline, but it permanently retains unverified prior coverage and all recipient holds. It does not backfill or claim earlier replies were checked. The baseline is captured before subsequent history walks, never used to reset an existing cursor after a gap. After baseline, use P3C3D2 bounded discovery; atomically persist deduplicated observations/events, terminal run and cursor only for complete unambiguous results. Provider failures, gaps, malformed/unattributed outcomes and identity/observation collisions preserve the old cursor and retain an explicit gap/coverage hold. Successful empty reads cannot clear holds or prior coverage gaps. Full bootstrap/backfill verification remains later and no completion path in this slice grants sending.

Read status locally with current authority and omit tokens, raw cursors, identity/provider IDs and snapshots. Verify denied roles/tenant/account/session, missing/oversized registry, claim/cooldown concurrency, exact-thread conflicts, stop/revocation/new-target races, expired recovery, replay/collision/rollback, numeric cursors and schema immutability/isolation. Run affected domain/provider tests, synthetic real workerd baseline validation, native D1 integration, fresh/repeat/populated migration proofs and final Worker build. Main data/credentials must be preserved except the additive empty schema. No new visual/browser requirement because no UI/route is added. One fresh Sol High review, at most one focused re-review.

### P3C3D3 local acceptance

Saved discovery runs are locally implemented, verified and independently reviewed:183 focused tests (including the new real workerd baseline),22 native D1 checks with100 targets/40 observations, final Worker build and migration0034 fresh/repeat/populated proofs pass. All95 prior application tables and credentials are preserved;35 domain migrations/63 domain tables now exist locally and all three new main discovery tables remain empty. One fresh Sol High reviewer found no material issue and independently passed23 mailbox tests; no re-review needed. Normal preview is restored with sending/checks/discovery disabled. No UI/route/job, live provider call, commit/push or deployment. BUILD_STATE owns the exact evidence.

### Next bounded task: P3C3D4 historical coverage and gap recovery

Define bounded backfill and recovery acceptance against registered accepted identities before implementation. The current profile baseline marks future progress only; retain explicit unverified historical coverage and sticky gaps until a separate verified coverage boundary exists. Preserve unmatched evidence without subject/address/domain attribution or silent cursor resets. Inspect existing exact-thread and history readers, immutable run targets and conservative holds for reuse. Keep broad live mailbox access, scheduling/follow-up sending, confirmed DSN classification, real imports/outreach and production off. A review UI and Results must describe actual coverage before offering any later authorized recovery action.

### P3C3D4A acceptance: bounded historical metadata collection

Implement the provider collection foundation first, without changing saved D3 progress or enabling an application caller. A future guarded recovery command must supply the full same-workspace/account accepted identity registry and persist the result under current authority, lease and target snapshots. This slice does not complete historical coverage or clear gaps.

List available messages from one second before the earliest accepted delivery's attempted time, using epoch seconds, includeSpamTrash=true and no sender, recipient, subject, label or thread filter. Read at most3 pages and40 unique message IDs, with a single15-second deadline and1MiB total budget; list pages and message metadata are each bounded64KiB. Deduplicate stable repeated IDs and reject conflicting thread identity, malformed pages, cycles or incomplete pagination. Read only the existing9 approved headers. No retry, redirect, body/snippet, external action, profile reset or progress update.

Reuse the registered exact ancestry matcher. Preserve strict history-reader behavior by default. For backfill only, separate valid incoming messages with no known root into unassigned server-only evidence proposals (provider message/thread IDs, received time and unreviewed kind). Matched observations remain tied to the accepted delivery; ambiguity, malformed ancestry or conflicting anchors invalidate the entire proposal. No raw headers, correspondence addresses, content or tokens in results. Terminal enumeration returns collected observations plus unassigned proposals, explicit unverified coverage and no next cursor. It never claims deleted mail was recovered, a transactional mailbox snapshot, history catch-up, a verified human reply or a confirmed DSN.

Verify registry isolation, derived time boundary, cross-thread/transitive matching, assigned/unassigned preservation, strict discovery regressions, pagination/dedup/limits, privacy, provider errors and total deadline in focused Node and real workerd interception tests. No live mailbox call or database fixture change. Final build and data/credential/source preservation checks; one fresh Sol High review and at most one focused re-review. No schema/UI/route change, so new migration/native D1/browser acceptance is unnecessary. Durable recovery runs, history catch-up and truthful review UI follow after provider acceptance.

Provider references: [message listing](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/list), [time-based queries](https://developers.google.com/workspace/gmail/api/guides/filtering) and [full versus incremental synchronization](https://developers.google.com/workspace/gmail/api/guides/sync).

### P3C3D4A local acceptance

Bounded historical metadata collection is implemented, locally verified and independently reviewed.130 focused tests pass, including20 new provider cases and one new real workerd test plus existing discovery/workerd/metadata/mailbox regressions. Final Worker build, syntax/diff and data/schema/credential/source preservation pass. One fresh Sol High reviewer found no material issue and independently passed58 backfill/discovery tests; no re-review needed. Normal preview is restored with sending/checks/discovery disabled. All98 application tables and credentials remain unchanged; no migration, UI/route/job, live provider call, commit/push or deployment. BUILD_STATE owns exact evidence.

### Next bounded task: P3C3D4B guarded durable recovery proposals

Define immutable recovery-run evidence and durable assigned/unassigned proposal storage under current authority, lease and complete delivery/identity/reply snapshots. Inspect reuse from D3 guarded runs and observations before choosing any additive schema. Define how history catch-up is tied to collection without silent resets, and what coverage remains unknowable for deleted or unavailable mail. Keep all recipient holds and existing gaps until separately verified review/coverage evidence permits an explicit action. Collection alone never grants sending or claims complete historical coverage. Keep broad live mailbox access, follow-up/scheduled sending, confirmed DSNs, real imports/outreach and production off.

### P3C3D4B acceptance: guarded saved recovery evidence

Reuse the D3 claim,90-second lease, cooldown, current session/member/original-authorizer/grant/sender guards, recipient holds and full target snapshots for a distinct recovery command. Require the existing exact workspace/account discovery gate plus explicit local recovery enablement. Add an immutable run kind, collection evidence and relational message proposals through one additive migration; no second mutable cursor or suppression ledger.

Capture a verified account profile history boundary before D4A collection, then collect bounded history changes from that boundary. Recheck current authority/lease/targets before each provider stage and completion. Each accepted provider stage retains its15-second/byte/page limits; the maximum three-stage walk fits the existing90-second lease. Preserve strict normal discovery behavior. The recovery-specific history reader may retain valid unassigned messages. Merge repeated message evidence only when assignment, thread, received time and classification agree; conflicting evidence yields no partial saved collection. A failed catch-up can save successful collection evidence with an explicit incomplete status, never a next cursor.

Save complete collection metadata, assigned/unassigned message proposals, deduplicated canonical observations, counts and safe activity atomically under the original run guards. No raw headers/content/addresses/tokens in proposals or activity. Immutable database guards prevent cross-workspace/account/run/target association, post-terminal insertion, count mismatch or cursor advancement by a recovery run. Current observations cannot be reassigned by collision. Preserve evidence and every existing recipient hold/stop; D3 cursors are never reset or advanced by recovery and coverage stays unverified/gap even when the catch-up walk completes. Permanently deleted mail and a transactional historical snapshot remain unknowable.

A current Owner/Admin read model returns only summary/time/count/status, with no raw provider IDs, history IDs or identity snapshots. No route/job/UI or broad live access is enabled by this domain slice. Verify incomplete/missing identity, gates/denied actors, stage/race/revocation/stop/lease checks, failures/partial catch-up, merge/conflict/replay, immutable schema/counts, atomic rollback and D1 parameter limits. Run focused provider/domain/schema tests, synthetic native D1, fresh/repeat/populated migration proofs, Worker build and data/credential/source preservation. One fresh Sol High review, at most one focused re-review. After acceptance, continue toward a truthful local recovery review surface; do not activate sending or broad live mailbox access.

### P3C3D4B local acceptance

Saved recovery evidence and catch-up pass174 focused tests,29 native D1 checks (100 targets/80 evidence records), final Worker build, fresh/repeat/populated/actual local additive0035 migration proof and preservation/hygiene. One fresh Sol High reviewer found no material issue and independently passed23 recovery tests; no re-review needed. All98 prior application tables/credentials are preserved;36 domain migrations/65 domain tables now exist locally. Main recovery tables remain empty and live flags stay disabled. No UI/route/job, live call, send or deployment. BUILD_STATE owns exact evidence.

### Next bounded task: P3C3D4C mailbox review surface

Expose a readable current-account Owner/Admin mailbox review screen and strictly guarded API around the accepted domain command. Show collected/unassigned counts, collection and catch-up outcomes and explicitly unverified historical coverage. Preserve existing navigation and keep provider IDs/history cursors/raw correspondence out of DTOs. Complete synthetic built-browser acceptance before any separately scoped live mailbox collection.

### P3C3D4C acceptance: current-account mailbox review

Add a focused full-page Owner/Admin review reachable from Overview and Sender setup. Reuse Bloomsi logo/layout, compact meaningful icons, restrained status colour, labelled facts, readable spacing and responsive stacking; no metadata chains, oversized metrics or unassigned-message content. Present latest attempt, available-message collection, catch-up status, assigned/unassigned counts and unverified historical coverage as separate facts. Never call enumeration full coverage or imply holds can be resumed.

GET is current-workspace/current-connected-account scoped and read-only. POST uses existing same-origin/session/access/body limits and the accepted recovery command; retain all domain gates, exact reviewed revisions/account and acknowledgement, no user-supplied query/cursor/target override. UI requires explicit read-only-check acknowledgement, prevents duplicate submissions, handles busy/cooldown/unavailable/missing-conversation-check/error states and refreshes status after any result. Missing identities direct users to existing conversation review. No send/clear/resume action. Main live recovery stays disabled.

Verify current/foreign workspace/account and denied roles/revocation, no read-side mutations or private DTO fields, strict POST/body/origin/revision/acknowledgement gates, disabled/busy/errors and keyboard focus. Run affected domain/navigation tests, final Worker build and real built-browser synthetic-provider flow at1440/1024/768/390/320px, including saved/empty/failed-catchup/unavailable states. Inspect captures and preserve main database/credentials/unrelated work. No schema change. One fresh Sol High read-only review, at most one focused re-review. A separately scoped live mailbox check remains the next acceptance boundary.

### Next bounded acceptance: controlled live mailbox review

The local surface is ready for a separately scoped real mailbox check. Read-only preflight finds three accepted test deliveries, all to the owner's test recipient, across three test prospects in the current workspace; the earliest accepted timestamp is2026-09-13T08:06:31.349Z. No delivery identity has yet been registered in the main local database. First refresh the existing grant if necessary and verify those exact accepted test conversations through the already accepted thread-check path, retaining any reply/stop holds. A missing/conflicting identity blocks collection; do not bypass it or resend.

The broader operation would read available metadata from hello@bloomwired.io since the earliest accepted delivery, including Spam/Trash and messages outside the test threads. Collection is limited to40 unique messages/3 listing pages with the existing bounded history catch-up. It sends no email and changes no Google messages. Local effects are immutable message-ID/time/assignment evidence, safe counts/status and recipient holds; no bodies/snippets are retained, operational cursor/coverage remain unresolved and holds are not released. No schedule, worker job, follow-up, import, production or deployment. Existing consent/test-conversation permission does not establish the owner's preference to include unrelated mailbox messages. Keep broader live flags disabled pending that scope decision; do not ask for another test message.

### P3C3D4C local acceptance

Mailbox review is accepted locally with43 focused tests,61 built-browser checks plus2 mobile-control checks, final Worker build and preservation of all100 main application tables/schema/ledger/credentials. One fresh Sol High review and the permitted focused correction re-review accept the final read-authority/account guards and unconditional Overview link. No schema change or live provider call in this slice. [Visual preview](../previews/prospecting-p3c3d4c/README.md); BUILD_STATE owns exact evidence. The separately scoped live boundary remains next.

### P3C3D4D controlled live prerequisite verification

The existing grant/account/sender check and three exact owner test-conversation checks pass. All three accepted deliveries now have verified immutable identities; the prior reply and hold remain. Six bounded provider requests, no retry/list/history collection/send. Only current-workspace check/token state, identities and activity changed; schema/ledger/other workspaces/credentials configuration remain unchanged. Main recovery tables stay empty and normal live flags remain disabled. No new implementation or independent-review cycle; accepted commands were exercised. Broader header collection, including unrelated mail since the earliest test delivery, remains the concrete next scope decision. See BUILD_STATE.

### P3C3D4D approved live collection acceptance

The owner approved one broader header-only collection including Spam/Trash and messages outside the test threads. The accepted recovery command read4 unique messages in7 provider requests, saved1 matched reply/0 unassigned and completed catch-up. The existing reply was not duplicated. All three conversations remain held, historical coverage unverified and operational/baseline cursors null. Eight built-browser read-only checks and desktop/mobile visual inspection pass. Schema/ledger/credentials/other workspaces are preserved; only expected evidence/holds/activity and subsequent local browser-session records changed. No send, mailbox mutation, migration, code change or extra review cycle. The normal preview remains disabled for further live checks. BUILD_STATE owns exact evidence.

### P3C3E1 acceptance: bounded delivery-status field parser

First implement a pure parser for a decoded ASCII message/delivery-status part. No MIME extraction, provider call, caller, persistence or automatic classification. Return only canonical RFC822 final/original recipients plus reported action/status facts; do not return diagnostics, MTA names, envelope identifiers or raw content. A parsed report remains untrusted and unassociated: no confirmed hard bounce, human reply, send permission or stop. Attribution to the exact verified accepted message and original recipient must be separately designed before use.

Use a documented conservative subset of [RFC3464](https://www.rfc-editor.org/rfc/rfc3464) and [RFC3463](https://www.rfc-editor.org/rfc/rfc3463): one required Reporting-MTA block,1..20 recipient blocks, required Final-Recipient/Action/Status, optional Original-Recipient. Accept folded fields and case-insensitive names/actions. Preserve action independently from status (failed4.x.x can report abandoned retries). Reject duplicates, misplaced standard fields, malformed/unsupported addresses/statuses, control/non-ASCII data and incomplete blocks with no partial output. Limits64KiB total,998 characters per physical line,8192 per unfolded value and64 fields per block. Known optional MTA fields require DNS syntax; Diagnostic-Code supports smtp plus nonempty diagnostic text; date fields support valid Gregorian dates with optional matching weekday and numeric timezone (years1900 onward, no leap-second syntax). Envelope/log IDs remain bounded opaque text, never correlation IDs. Unknown bounded extensions are ignored, never treated as correlation evidence. Raw unfolding retains whitespace for the8192-character limit before normalization. Internationalized/global DSNs and quoted/commented mailbox grammar remain unsupported.

Verify valid/multi-recipient/folded cases, unknown extensions, failed/delayed/success status facts, ambiguous duplicates, unsupported grammar, injection/size/recipient limits and privacy. Run focused parser and existing reply/discovery regressions, preserve data/credentials/source and verify import/syntax. There is no application caller or UI/schema change, so no build/browser/migration rerun is required for this isolated parser. One fresh Sol High read-only review of security-sensitive evidence parsing, at most one focused re-review. Keep real provider/body access and all sending disabled.

### P3C3E1 local acceptance

The pure field parser passes139 focused tests (54 new parser,85 existing metadata/discovery regressions), syntax/import/diff and preservation of all100 application tables/schema/ledger/credentials/unrelated source. One fresh Sol High review plus its single focused correction re-review accept the final parser; the reviewer independently passed54 parser tests. No caller/provider/UI/storage/migration or automatic classification. Raw unfolded whitespace and recognized optional standard grammar now have explicit regression coverage. Existing accepted build remains current; no unnecessary build/browser rerun. BUILD_STATE owns exact evidence.

### Next bounded task: P3C3E2 MIME evidence and original-message association

Inspect bounded extraction of delivery-status and returned original-header MIME parts before adding transport or persistence. Association must use the exact same-workspace/account accepted delivery's verified returned message identity and original immutable recipient; address, subject, daemon name, envelope ID and thread proximity alone cannot establish it. Treat parsed fields as untrusted claims, preserving ambiguous/unsupported reports for review; identity correlation alone cannot authenticate a DSN. Define synthetic size/depth/encoding/content-type/multi-recipient/mismatch/forgery acceptance first. Do not read additional live message bodies, create automatic stops, release holds, send, deploy or broaden the approved one-run mailbox scope.

### P3C3E2 acceptance: bounded MIME report association

Add a pure interpreter of a supplied Gmail Message MIME tree, reusing E1 fields and the existing complete registered identity validation. The future guarded caller must supply a server-read candidate's exact workspace/account/provider message/thread/time and complete accepted-delivery registry. No provider or attachment fetch, database write, route, UI or automatic verdict. Keep the normal live flags off.

Require a top-level multipart/report with consistent Content-Type/report-type=delivery-status, a bounded human-readable first part, one inline message/delivery-status second part and returned original headers in the third part. Support inline text/rfc822-headers and bounded raw message/rfc822 (inspect its header section only). Reject external evidence attachments, nested report/embedded-message alternatives, missing/extra/reordered evidence and conflicting duplicate headers/parameters. Human plain/HTML or multipart/alternative data is never interpreted as evidence. Gmail body.data is decoded once from canonical base64url, with declared byte size checked for interpreted bodies; MIME transfer-encoding metadata does not trigger another decode. Limits:16 parts,depth4,64 headers/part,8192 raw characters/header,256KiB combined recognized header/encoded-body data,64KiB decoded evidence/part and16KiB returned headers. Both padded and unpadded canonical base64url are supported.

Require incoming non-DRAFT/non-SENT metadata to equal the saved candidate. Returned Message-ID must equal exactly one verified returned identity; returned From/To must equal its immutable sender/recipient. No submitted-ID, address, subject, envelope-ID, daemon-name or thread-only fallback. One-recipient accepted messages require one matching final recipient and, if present, the same original recipient; forwarded/extra/mismatched recipients remain unresolved. Report time cannot precede accepted send attempt. Conflicting known outer ancestry, original recipient headers or reused sent/provider identities fail closed. Parsed correlation remains explicitly unverified authenticity and held; even convincing forged reports cannot produce a hard-bounce/stop/send verdict. Return only safe association/action/status/time facts, never addresses, raw headers, diagnostic strings or content.

Test successful header/full-message forms, quoted content parameters, alternate human content, encoding/size/depth/field/part limits, duplicates, missing and conflicting identity/context/time, cross-workspace/account, multiple/forwarded recipients, forged reports and privacy. Run affected parser/metadata/discovery/backfill/recovery regressions, final Worker build and data/credential/source preservation. No schema change or UI/browser rerun. One fresh Sol High read-only review, at most one focused re-review. Sources: [Gmail message parts](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages#MessagePart), [part body encoding](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages.attachments#MessagePartBody), [report structure and forgery](https://www.rfc-editor.org/rfc/rfc6522).

### P3C3E2 local acceptance

Bounded MIME extraction and exact accepted-message/recipient association pass277 focused tests, final Worker build and preservation/syntax/diff checks.72 association tests also pass after the final uncalled-module MIME-type length guard. One fresh Sol High read-only review found no material issue and independently passed72 tests; no re-review needed. All100 application tables/schema/ledger/credentials/unrelated source unchanged. No provider reader, application caller, persistence, UI, migration or automatic bounce/stop action. Normal preview is restored with live flags disabled; BUILD_STATE owns evidence.

### Next agent-owned task: P3C3E3 exact-candidate provider retrieval

Implement the bounded provider adapter around E2 for a caller-owned exact saved report candidate. Reuse existing Google read transport with a fixed original-account message endpoint, explicit response fields, one timeout/byte budget and no redirect/retry/attachment follow-up. Reject invalid context before transport and require the response to match the saved candidate before returning E2's held/unverified proposal. A future guarded domain caller remains responsible for current authority, grant, lease and durable evidence. Verify synthetic provider failures/partial bodies, limits, request privacy and true Worker runtime before connecting any route or live body access. No owner setup is needed for this local code; another live mailbox read, send or deployment is not authorized by the adapter's implementation.

### P3C3E3 acceptance: exact saved-report provider reader

Add an uncalled provider-only adapter using the shared bounded Google JSON transport and E2 interpreter. Preflight the complete registry and same-workspace/account saved candidate before any request; require canonical received time, safe provider identifiers and a candidate that is not a registered sent message. Snapshot caller-owned context before awaiting transport. The future domain command owns active authority, original-account token/grant, saved provenance, lease and post-read revalidation; this adapter cannot establish them or grant live access.

Make one fixed GET to `/gmail/v1/users/me/messages/{savedId}`, format full, explicit fields `id,threadId,labelIds,internalDate,payload`; retain the full supplied MIME tree for E2 depth/shape rejection. No root snippet/raw/history field, request body, query override, listing/profile request, redirect, retry, refresh or attachment follow-up. One15-second deadline covers headers and body, with256KiB UTF8 response limit including unknown JSON fields. Reject non200, malformed/partial/oversized/error/stalled bodies without content or partial evidence. Return only E2 held/unverified association facts. Bodies are ephemeral, never logged/persisted or included in output. The API's authenticated `me` resource is documented in [Google messages.get](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/get); matching that token to the saved original account remains the guarded caller's duty.

Verify invalid preflight produces zero requests; exact request/privacy, in-flight caller mutation, all provider failures, byte-boundary/UTF8 accounting, external attachments and saved-message mismatches. Bundle and execute in real workerd with synthetic intercepted egress, including delayed headers then stalled body under one deadline. Run E1/E2 and affected transport/discovery regressions, syntax/diff and main database/schema/ledger/credential/source preservation. There is no application caller, changed app import path, UI or schema, so this pure adapter's real Worker bundle/runtime proof replaces an unrelated full application build; browser/migration reruns do not apply. One fresh Sol High read-only review, at most one focused correction re-review. Keep normal preview/live flags unchanged; no real body read, send, deployment or video work/tests.

### P3C3E3 local acceptance

The uncalled exact-report adapter passes303 distinct checks (302 Node plus1 real workerd), syntax/diff and preservation of100 main application tables/ledger/schema/credentials/unrelated source. One fresh Sol High read-only reviewer found no material issue and independently passed97 provider/association tests; no re-review needed. Real Worker bundling/runtime covers the uncalled modules; existing app build/preview remains current. No UI/schema/caller/live request. BUILD_STATE records evidence and the runtime-metadata comparison limitation.

### Next agent-owned task: P3C3E4 guarded durable report evidence

Define and implement a bounded domain command for one exact server-read saved recovery candidate, using current same-workspace Owner/Admin authority, the original account's current grant, full registered delivery snapshots and claim/completion revalidation. Persist only immutable safe action/status/association evidence, never raw report bodies/headers/diagnostics. Reuse existing lease/hold/recipient-protection patterns; preserve existing recovery records, observations, manual stops and coverage gaps. Treat association as unauthenticated and keep holds; explicit review/confirmed bounce classification remains separate. Complete synthetic race/isolation/replay/rollback/native-D1 and migration acceptance before exposing any route or enabling a live full-payload read. No owner setup is needed for local implementation.

### P3C3E4 acceptance: guarded durable report evidence

Add a local-only Owner/Admin command for one immutable saved recovery message of kind delivery_report/needs_review. Exact workspace/account/candidate configuration and explicit reviewed acknowledgement supplement the existing discovery gate. Reuse active session/member stamps, original-account healthy token access and grant/sender/authorizer guards. Require complete1..100 verified accepted deliveries, settled discovery/thread leases and already held/stopped recipients. Snapshot registered delivery identities and reply revisions relationally; atomically claim one report check per account, increment recipient revisions while preserving holds/stops, and reject competing checks. One90-second lease and30-second completed-check cooldown bound retries; supersede expired checks without changing their provenance.

Revalidate current authority/account, the complete accepted set, saved candidate/previous assignment, discovery revision and every reply revision before transport and completion. A later manual stop, check, revocation, changed account/target set or expired lease discards late evidence. One E3 read supplies only held/unverified association facts; mismatched prior assignments or conflicting canonical observations remain unresolved. Persist an immutable terminal check with safe association/action/status columns and immutable relational targets, atomically with activity. Do not change existing recovery assignments/observations/cursors/gaps or invent verified authenticity. Successful checks deduplicate across repeated collections by original account/provider ID; unresolved checks may be explicitly retried after cooldown. No body/diagnostic/header/recipient/token storage, automatic stop/hold release, route/UI or live enablement.

Add an additive migration with same-workspace foreign keys, one-active-check uniqueness, immutable provenance/targets/terminal evidence and safe terminal-field constraints. Verify synthetic authorization/preflight/revocation/account/target/reply races, expired lease recovery, concurrent claims, replay, provider failure, assignment conflicts, privacy,100targets and atomic rollback; exercise real local D1. Run focused regressions, Worker build, fresh/repeat/populated-copy/local migration and preservation checks. One fresh Sol High read-only review, at most one focused correction re-review. Existing normal preview stays disabled for live actions; original LTB and all old rows/voices remain preserved.

### P3C3E4 local acceptance

Guarded report evidence passes230 distinct focused tests,19 native D1 checks, Worker build, fresh/repeat/populated-copy/actual local migration and preservation checks. One fresh Sol High read-only reviewer found no material issue and independently passed152 report/provider/association/schema tests; no re-review needed. Migration0036 adds two empty local tables; all100 prior application tables/rows, credentials and unrelated source are preserved. Normal built preview is healthy with live actions disabled. No UI/route/provider enablement, real request or send. BUILD_STATE owns exact evidence.

### Next agent-owned task: P3C3E5 delivery-report review surface

Build a scoped current-account Owner/Admin read model and guarded local review screen/API around saved recovery candidates and E4 results. Reuse Bloomsi navigation/layout, readable labelled facts, meaningful compact icons and explicit empty/unresolved/associated/busy/disabled states. Association remains unauthenticated and held, never confirmed hard bounce or sending permission. Keep raw provider IDs, correspondence, tokens and body content server-side; resolve selection against immutable saved candidates without trusting client-supplied provenance. Revalidate current authority/account after asynchronous reads, apply existing same-origin/body/revision/acknowledgement controls and test stale/foreign/denied states. Complete synthetic desktop/mobile built-browser, focused domain/regression and final build/review acceptance; no live body read or owner setup is needed to implement this surface.

### P3C3E5 acceptance: saved report review

Add `/prospecting/mailbox/reports` from Mailbox review, with Overview navigation retained. Show up to50 distinct newest saved current-account delivery_report/needs_review candidates and latest safe report-check outcomes. Deduplicate repeated collections by provider message, preferring newest saved recovery provenance. Resolve browser selection using a SHA256 reference over workspace/account/run/message; raw provider IDs stay server-only, and a reference is never authority. GET and POST reuse current mailbox authority/account revalidation and E4's exact original-candidate/current-grant command. Strict4096-byte JSON, same-origin, current revisions, explicit body-read acknowledgement and server resolution precede a check. No client query/body/token/recipient override.

Use existing Bloomsi typography, layout, garden/favicon avatar and compact controls. Keep incoming time, association, reported outcome and held/unverified meaning distinct, with a short explanation and optional detail. Empty/disabled/busy/cooldown/needs-connection/unresolved/associated, focused error/reload, duplicate submission prevention and keyboard access are required. This view does not confirm hard bounces, clear holds or send. Synthetic built-browser tests must cover1440/1024/768/390/320, errors/loading/reduced-motion and role/origin/input/selection/privacy guards. Run focused domain/navigation regressions, final Worker build, main data/credential/source preservation and one Sol High review (one correction re-review maximum). No schema change, live mailbox/body read, email, deployment or video tests.


### P3C3E5 local acceptance

Current-account Delivery reports UI/API passes72 distinct focused tests,61 built-browser checks, final Worker build and preservation/syntax/diff checks. One fresh Sol High review found no material issue and independently passed11 tests; no re-review needed. Existing favicon/garden behaviour is verified using real loopback images, avoiding Playwright's favicon interception. No schema, live read/send or deployment. All main application data/credentials and unrelated source preserved. BUILD_STATE owns evidence and preview.

### Next agent-owned task: P3C3E6 report-linked human stop review

Connect an associated permanent-failure report to the existing human hard-bounce stop decision, without treating the report as authenticated evidence. The reviewer must explicitly acknowledge personal review and supply a bounded evidence note. Reuse the existing authoritative, revision-bound permanent stop command and recipient-wide protection; the action sends nothing, performs no provider read and never resumes outreach. The report association remains immutable and unauthenticated after the human decision.

Resolve an opaque report selection against current authorized saved candidates. Only an associated failed5.x report for the exact accepted delivery/prospect/account can be linked. Reject stale reply revisions, wrong workspaces/accounts/targets, delayed/delivered/unresolved reports, missing acknowledgement/note and duplicate stops. Guard report identity, current membership/session and reply revision in the atomic stop transaction. Store the safe report-check reference in existing append-only stop activity provenance; existing relational reply stop state remains the operational source of truth, with no new schema or hidden JSON state machine. Preserve the independent manual-stop path.

Use a compact named disclosure on the report result, with clearly labelled evidence and acknowledgement, a distinct permanent-stop button and a saved human-decision state. No automatic preselection/confirmation. Keep website favicon/garden identity and separate reported outcome from human stop. Verify synthetic domain authority/races/rollback/recipient protection, actual built desktop/mobile keyboard/error/loading/saved states, final Worker build and source/data/credential preservation. One fresh Sol High review, at most one focused re-review if needed. No live mailbox/body read, send, deployment, import or video tests.


### P3C3E6 local acceptance

Report-linked human stop review passes101 distinct focused tests,52 built-browser checks, final Worker build and preservation/syntax/diff checks. One fresh Sol High review found that the public manual-stop endpoint could accept report provenance. The correction separates strict manual input from server-resolved report provenance; unit and real built-API regression checks pass. The single focused re-review accepts the fix and independently passed41 affected tests. Native D1 commits one permanent stop and provenance event; no provider request, schema change or real stop. All main data/credentials and unrelated source are preserved. BUILD_STATE owns evidence and visual preview.

### Next agent-owned task: P3C3F mailbox coverage and hold resolution

Define and implement an explicit distinction between a bounded mailbox collection, the start of future monitoring, historical coverage and a human decision about an existing conversation. A completed catch-up or report confirmation cannot silently verify missing older mail or release a hold. Reuse saved recovery/discovery provenance and registered immutable delivery identities; account/workspace/grant/revision changes and missing/expired history invalidate readiness. Preserve all reply/stop observations and the permanent recipient-wide stops. Require actual evidence for any operational coverage promotion, and keep any manual attestation labelled as human evidence rather than provider verification. Do not activate follow-up sending while coverage/reply decisions remain unresolved.

Implement and verify the local path before requesting any real mailbox scope or human review. Gmail documents full synchronization after unavailable history; a stale-history404 is not an empty result. See [Google synchronization guidance](https://developers.google.com/workspace/gmail/api/guides/sync) and [history.list](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.history/list). No additional provider access, deployment or owner setup is authorized merely by defining this next local task.


### P3C3F1 acceptance: reviewed monitoring checkpoint

Save an explicit local monitoring checkpoint from the latest completed recovery collection (at most five minutes old), with complete catch-up, zero unassigned messages, the same current account/grant/sender revisions and exact1..100 registered accepted targets/reply revisions. Require current Owner/Admin session/member authority, exact local discovery workspace/account enablement plus a disabled-by-default checkpoint gate, current revisions and an acknowledgement that this establishes a future starting point only. Resolve the source through an opaque server selection; no raw provider cursor or source override in the browser. No provider read, automatic monitor/job, send, human attestation of unseen mail or release of any hold.

Use existing discovery claims, targets and terminal guards. A new checkpoint run records immutable relational provenance to its original recovery collection. Advance only the operational cursor to that collection's completed catch-up cursor, preserving any earlier baseline, gap/older-mail uncertainty, all original runs/messages and all held/stopped decisions. Reject replay, backwards cursor movement, stale/new targets, expired/busy leases, revised replies, changed account/grant/sender/member/session and failed completion. Late results cannot be reported as saved; completion, its activity and cursor changes roll back together. A claimed but unfinished attempt remains traceable. Additive migration and SQL constraints protect the new provenance and permitted run kind; recovery itself still cannot advance progress.

Show the available collection, saved future starting point and older-mail uncertainty separately in Mailbox review, with clear disabled/expired/changed states and explicit acknowledgement. Verify domain races/replay/rollback/privacy/denied access, fresh/repeat/populated/local migrations, native D1, built desktop/mobile/keyboard/error/loading/duplicate-submit behaviour, final build and main data/credential/unrelated-source preservation. One fresh Sol High read-only review, at most one focused correction re-review. This completes only the checkpoint foundation; verified historical coverage, held conversation resolution and follow-up sending remain separate unfinished work.


### P3C3F1 local acceptance and owner pause

Checkpoint foundation accepted locally after122 distinct focused tests,15 native D1 checks,56 built-browser checks, final build and migration/preservation proofs. One Sol High review and its focused correction re-review accept the inclusive five-minute cutoff. Browser coverage predates that server-only cutoff correction; affected domain/schema/native checks and build were rerun. No real checkpoint/provider request or hold release. The owner then explicitly paused further email work and directed progression to other phases. P3 historical coverage, hold resolution, follow-ups and Results remain unfinished; P4 Pages becomes active. BUILD_STATE owns evidence.
