# P4 — main Pages

The owner paused further P3 email work on13 September2026 and directed progress to other phases. P3 remains unfinished; P4 is active under the approved Prospecting roadmap.

## P4A — create, edit and save workspace pages

Replace main Pages' placeholder with a real workspace page list and full-page editor. Reuse RichEditor and its supported rich text/block/formatting behaviours. Existing PageView couples legacy sharing and save semantics to legacy APIs, so compose the editor inside a Bloomsi page wrapper. Keep all original pages and legacy routes intact; copy/import nothing implicitly.

Use a separate canonical workspace table with revision-bound updates and duplicate-safe create requests. Initially internal Owner/Admin authoring only; no portal/client/public sharing, visibility controls or operational database data. Denied/foreign/suspended access must be enforced in reads and writes. Record safe activity without page content. Do not mount legacy database view readers inside Bloomsi; retain block configuration while showing an unavailable view. Preserve legacy editor defaults.

Provide an icon-led page list, readable inline title, borderless writing canvas, slash/block insertion and quiet honest saving feedback. Autosave changes serially, preserve edits typed during a pending save, and keep a recoverable browser-tab draft after a network failure or conflict. Never silently overwrite another revision. Scope recovery to the same authenticated user, workspace and page; offer explicit retry, latest-version review and a draft download. Handle storage failure and navigation before a pending save honestly. Editor code loads only on the editor route.

Verify strict input/size/revision/idempotency/authorization and activity rollback using focused tests; exercise create/edit/reload, rapid edits, network failure, conflict/recovery, rich content round-trip, keyboard and mobile in the built Worker. Run affected editor/tree tests without video work/tests, additive fresh/repeat/populated/local migration proofs and preservation checks. One fresh Sol High review applies to this schema/authority integration, with one focused re-review if needed.

Nested page movement/tree UI, search, inline icon selection, larger document compatibility, attachments and authorized sharing/projections remain later P4 slices. P4A does not claim full Notion parity or all P4 acceptance.


## P4A local acceptance

Create/open/edit/save and tab-local draft recovery are accepted locally after103 focused checks,45 built-browser checks, final Worker build and migration/preservation proofs. One fresh Sol High read-only review found no material issue and independently passed21 domain/draft tests. A final copy-only leave-page warning clarification is included in the last build; the browser run predates that wording change. No re-review needed. Main schema has39 migrations and the new workspace Pages table is empty; all original records/schema/credentials remain intact. BUILD_STATE owns evidence.

## P4B nested pages and page finding

Reuse `lib/page-tree.mjs` for a readable page tree and breadcrumbs. Add explicit same-workspace child creation and move/reorder commands, preserving depth/cycle/orphan safeguards and revision conflicts. Keep parent and ordering state relational, preserve all bodies and untitled drafts, and provide keyboard/touch alternatives to dragging. Add bounded title search with a clear return to the hierarchy. Continue Owner/Admin authoring; client/public sharing, legacy document copies, operational projections and all email work remain outside this next slice. The implementation acceptance is recorded below.

## P4B implementation acceptance

Follow familiar Notion page navigation within Bloomsi: a persistent expandable page sidebar, compact document rows, inline child creation, breadcrumbs and child-page links beneath the writing canvas. Provide a Move to dialog with a searchable destination and explicit sibling position; dragging onto a page nests it, while the dialog works with keyboard/touch. Mobile uses a labelled collapsible page navigator, not a squeezed second sidebar. Title finding shows at most50 results and a clear return to the tree. Keep the editor lazy-loaded and preserve existing autosave/recovery.

Store hierarchy separately from document content. Workspace-wide hierarchy revisions serialize create/move/reorder decisions; moving a page never increments its document revision or changes title/body. Check current Owner/Admin authority and workspace inside each atomic write. Same-workspace parent constraints, five-level subtree depth/cycle checks, stale-tree rejection, duplicate-safe creation and activity rollback must hold. Existing documents become top-level in deterministic order through an additive migration; no copies or deletions.

Acceptance requires focused domain/race/authorization/tree/draft tests, actual Worker browser coverage (navigation, nesting, drag, move/reorder, search, conflict, draft preservation, keyboard/mobile at1440/1024/768/390/320), build, additive fresh/repeat/populated/local migration and preservation proofs, and one fresh Sol High read-only review. Larger Notion features, icons/covers, attachment compatibility and sharing remain later slices. No email/provider action, deployment, commit/push, video work or legacy import.


## P4B local acceptance

Accepted locally after118 focused tests,48 final hierarchy browser checks,45 final editor regression checks, final Worker build and fresh/repeat/populated/local migration proofs. One fresh Sol High read-only reviewer found no material findings and independently passed50 focused hierarchy/page/draft/tree tests. No re-review needed. All104 preexisting application tables/rows and old schema, credentials and unrelated work are preserved. Normal preview runs the final build on8787; isolated8788 is stopped. No commit/push or deployment. BUILD_STATE owns detailed evidence.

## Next: P4C inline page icons

Reuse the existing SVG icon picker for explicit inline page-icon selection and consistent document/sidebar/breadcrumb identity. Follow the owner's no-emoji preference and preserve writing, hierarchy and draft recovery. Define bounded schema/save/compatibility acceptance before implementation. Broader rich-content/attachment compatibility and permissioned sharing/projections remain later P4 work; P3 email remains paused and P5 handoff remains unfinished.

## P4C owner expansion — inline icons and named page sharing

Owner direction: "keep going til i am actually needded ... share the pages and subpages to clients and users ... copy notion features". Named sharing is authorized now alongside inline icons. Implement the familiar Share dialog with current workspace recipients, Can view / Can edit / No access, inherited subpage permissions, explicit inheritance restriction, copy link and revocation. Sharing grants target verified active memberships; client grants also bind an existing linked client contact. Private ancestor/sibling names, IDs, bodies and membership details must not leak through tree, breadcrumbs, search, reader, APIs or portal. Owners/Admins retain administration. Shared editors can change writing, but cannot change hierarchy or sharing.

Use nearest-ancestor permissions, including explicit No access overrides. A page can stop inheriting; descendants inherit from that new boundary. Current membership, role and linked client contact must be rechecked in every read/write. Sharing/inheritance changes serialize with page movement through the hierarchy revision. Moving between parents when sharing exists requires explicit acknowledgement before changing inherited access. No data is copied. Icon choices use existing native SVG glyphs, with a compact accessible picker; the inherited picker is emoji-only and cannot satisfy the owner's no-emoji direction.

Client recipients open documents under portal Pages; internal recipients use Pages. Copied links require sign-in, resolve the authorized workspace, and ask before switching workspace context. Public anonymous publishing, actual invitations/messages and external guests without existing membership remain separate from named sharing. Preserve body revisions and draft recovery, including permission revoked during an in-flight save. Read-only documents reuse the established HTML sanitizer; no operational data readers.

Acceptance: focused access/inheritance/revocation/contact-change/role-change/move/concurrency/rollback/icon tests; server route/shell checks; built Worker flows for Owner, internal view/editor and two isolated clients, private parent filtering, revoked access and pending drafts; keyboard and five viewport widths; build, additive fresh/repeat/populated/local migration and preservation proofs. One fresh Sol High read-only review, with at most one focused re-review. No production/DNS, provider/email action, commit/push, video tests or legacy import.

## P4C local acceptance

Accepted locally after161 focused tests,143 built-browser checks (50 sharing,48 hierarchy,45 editor), final Worker build, fresh/repeat/populated/local migration and preservation proofs. One fresh Sol High reviewer found no material findings; no re-review. Named client/user sharing and inherited subpages work; public anonymous publication and all Notion parity are not claimed. BUILD_STATE owns evidence.

## P4D document discussions and comment-only access

Implement page-level comment threads and replies behind a compact Comments control, with Open/Resolved filters and resolve/reopen. Use plain text, authenticated display names and timestamps. Can comment inherits using the existing nearest-grant rules; view can read discussions, comment/edit can post, and editors or the thread starter (while still allowed to comment) can resolve/reopen. Comments do not grant document editing, sharing or hierarchy authority. No inline text anchors, mentions/notifications, public publication or email in this slice.

Each read and atomic write rechecks current page access, membership/contact and workspace. Composite tenant/page/thread constraints, strict size limits, duplicate-safe post requests, revision-bound thread updates and activity rollback are required. Existing grants, page content, hierarchy, all unrelated records and credentials remain intact. Preserve an unsent comment in the mounted form on failure; retrying an ambiguous response must not duplicate it. Explicit pagination avoids silently hiding old threads/replies.

Acceptance: focused domain authorization/revocation/inheritance/concurrency/rollback/pagination/input tests, built Worker owner/client flows and request denials, five widths and keyboard, actual build, fresh/repeat/populated/local migration and preservation proofs, one fresh Sol High read-only review. No commit/push, deployment, provider action, video work/tests or legacy import.

## P4D local acceptance

Accepted locally after176 focused tests and179 built-browser checks (36 discussions,50 sharing,48 hierarchy,45 editor). Final Worker build, fresh/repeat/populated/local migration and preservation proofs pass. One fresh Sol High read-only review found no material findings; no re-review needed. Named comment access, replies and resolved discussions work. No external actions or legacy copies; normal preview restored, all live flags disabled. BUILD_STATE owns detailed evidence and remaining scope.

## P4E rich document reader compatibility

Retain the inherited editor and source HTML. Opt main Bloomsi's authorized read-only reader into supported native toggles, 2–4 responsive columns, safe inline raster images, headings/lists/tasks/tables/callouts/code and server-rendered equations. Preserve the original sanitizer defaults and legacy readers. Retain safe bookmark links for embeds; iframe/video activation and video tests remain paused. Do not fetch provider data or enable operational data readers.

Run sanitization before equation rendering. Inline image capability applies only to img src with bounded strict raster base64; never data links, SVG, executable markup or arbitrary styles. Current named membership/page access must precede body delivery. Rendered HTML is derived only and never replaces stored source. Source documents/copies, attachment storage and new schema are outside this compatibility slice.

Acceptance: focused sanitizer/formatting/equation/permission/storage-preservation tests with invalid inputs and unchanged legacy defaults; built Worker rich-content round trips, client/viewer rendering, keyboard toggles, inline image round trips, responsive columns/tables at five widths and no provider/legacy records; relevant regression checks/build/preservation. One fresh Sol High read-only review applies to the rendering boundary. No migrations, real shares, external actions, email or video tests.

## P4E local acceptance

Accepted locally after125 focused tests and39 final built-browser checks, final Worker build and full source/data/schema/credential preservation. One Sol High review found serialized-equation comparison/entity issues; quote-aware opt-in scanning and inert equation-attribute normalization fix them. Six real-serializer/safety regressions and browser comparison/matrix checks pass; the single focused re-review accepts the fix. No migration or external action. Normal preview restored, all live flags disabled; BUILD_STATE owns detailed evidence and remaining scope.

## P4F accessible document block movement

Add opt-in movement controls to Bloomsi's existing RichEditor: labelled SVG up/down buttons and Ctrl/Cmd+Shift+ArrowUp/ArrowDown shortcuts. Move the current top-level document block, preserving nested lists/tables/columns as a unit, marks, selection and undo/redo. Reject boundary moves and selections spanning multiple top-level blocks without changing content. Keep existing drag support and legacy editor defaults. Reuse serial autosave/recovery; no new save path, permissions or schema.

Acceptance: pure ProseMirror transaction tests for order, nested content, forward/reverse selection, node selection, boundaries/multiblock rejection and undo/redo; built Worker real keyboard/button/touch movement, saved order/reload/undo, existing drag behaviour, narrow layout and scoped source/data preservation. No email/video work/tests, provider actions, new migration or deployment. Follow selective review triggers; routine editor-only changes do not automatically require a reviewer.

## P4F local acceptance

Accepted locally after24 focused tests,36 final movement browser checks, Worker build and preservation.45 editor/recovery checks passed before the gap-cursor-only fix. One Sol High review identified a gap-selection mapping issue; the fix, real StarterKit/browser regressions and single focused re-review pass. No migration or external action. BUILD_STATE owns evidence.

## P4G native page links

Reuse the inherited @ mention picker for Bloomsi page links from the current authorized page tree. Insert ordinary labelled links to the existing shared-page resolver with explicit workspace context. The picker shows pages only, uses a real input label, supports keyboard/touch and remains within narrow viewports. Preserve legacy mention defaults and existing editor/autosave behaviour. Selecting a page inserts a static document link; it never grants access or invites anyone. The destination independently checks current membership/page permission and routes clients to portal Pages. No bodies or hidden ancestor/sibling metadata in suggestions.

Acceptance: focused link-building/filtering and unchanged legacy tests; built Worker insertion, keyboard/Escape/email text, reload, client authorized navigation, denied private target and unauthorized suggestion omission, five widths, editor lazy loading and data/source preservation. Reuse existing routes and authority; no migration or external action. One Sol High review applies to integrating links across authorized picker/editor/client routing. No anonymous publication, operational projections, email or video work/tests.

## P4G local acceptance

Accepted locally after72 focused tests and72 browser checks (27 linking,45 editor/recovery), final Worker build and preservation. One Sol High read-only review found no material findings and independently passed57 focused tests; no re-review. No schema or external action. BUILD_STATE owns evidence.

## P4H remaining Pages compatibility and navigation acceptance

Verify existing authorized file-download links inside shared documents using synthetic project files, including client visibility, foreign/unrelated denial and revocation. Do not grant file access from a page grant, copy source documents, add a new upload/storage system or activate video/iframe work. Exercise retained non-video legacy tree/sharing and supported editor/reader tests.

Compare pre-Pages and current builds on the same synthetic representative workspace and accepted full-content/two-frame navigation method, with40ms local D1 latency. Measure Home/Clients/Work at desktop/mobile, request/statement/call counts and loaded JavaScript; assert no Page/editor/legacy operational dependency on unrelated routes. Preserve samples and explain frame-sized/noisy timing differences. Reconstruct the pre-Pages source only in a disposable evidence directory and prove runtime-source hashes; never revert the working branch. Record limitations instead of claiming stale measurements are current.

No production code changes planned; fix only confirmed blockers if needed. Keep normal local data/schema/credentials intact and restore its built preview. One Sol High review applies to access/performance acceptance. Anonymous publication and live operational projections remain separately scoped; no external action, email, video tests, commit/push or deployment.

## P4H local acceptance

Accepted locally after192 compatibility tests,15 file-link browser checks and a controlled pre-Pages/current navigation comparison (84 samples,60 retained,12 cold loads). Request/statement/call counts are unchanged; shared JS increases353 decoded bytes, with no editor runtime or Pages-table queries on unrelated routes. Baseline source matches1785 recorded hashes. One Sol High review found no material findings and independently passed192 tests. No runtime or schema changes; BUILD_STATE owns evidence.

## P4I linked Work views

Reuse Table, Board, Calendar and Gallery presentations for bounded read-only projections of canonical Actions. Store only source/layout/status-filter configuration in the existing document block. No copied records or operational writes. Every query intersects current Page access and canonical Action authority in SQL; client page grants never expose internal Work. Reuse existing record destinations. Preserve legacy database views and editor defaults, document serialization, autosave and lightweight readers.

Provide compact icon-labelled layout controls, a labelled status filter, explicit 50-row pagination, refresh and clear loading/empty/error/unavailable states. Calendar explains that dates/counts cover the current result page. Reader controls are temporary; editors can save layout/filter using normal document revisions. Validate source/configuration and request workspace; no anonymous reader or new sharing grants.

Acceptance: focused actual-SQL access/revocation/tenant/filter/pagination tests and legacy/editor regressions; built Worker four layouts, source-only configuration round trip, owner/team/client authority, record links, no operational writes, keyboard and five widths; final build and source/database/credential preservation. One fresh Sol High read-only review with at most one focused re-review. No migration, email/video work/tests, live provider actions, commit/push or deployment.

## P4I local acceptance

Accepted locally after153 distinct focused tests,55 linked Work browser checks,45 editor/recovery checks,39 rich-reader checks and6 final-copy browser checks. Final Worker build passes. A42-sample navigation comparison retains request/SQL/call counts and excludes Page queries/editor runtime on unrelated routes; shared JS adds153 decoded bytes. One Sol High read-only review found no material findings; no re-review. All original data/schema/credentials and unrelated source remain preserved. BUILD_STATE owns detailed evidence and limitations.

P4's bounded core editing, named sharing and canonical Work projections are locally accepted. Anonymous publication/external guests, larger database relations and full Notion parity remain separate. No merge/deployment claim. Next agent-owned phase: P5, starting with a read-only conversion preflight; email and video remain paused.
