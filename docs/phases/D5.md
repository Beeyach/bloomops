# D5 — Launch + Handoff

Base: verified D4 merge `cae247fa823e7a54ffb6632a0e517ce650483e9f`.
Continue under the owner's instruction until actual input is needed.

Complete and retain an integrated GHL and Kajabi operating story from selected
build work through QA, Client Review, Launch and Handoff. Reuse canonical Actions,
Milestones, Deliverable approval/delivery, private R2 Files and their existing UI.
A handoff document attaches to an existing or manually created Deliverable; this
is sufficient structured scope, so add no handoff table or general Pages feature.
No runtime feature change is required unless acceptance exposes a concrete gap.

## Operating contract

- QA work does not approve an output. An authorized internal coordinator records
  Deliverable Internal Review, Client Review, Approved and Delivered as applicable.
  Client Review is an operational status, not a new client approval endpoint.
- Launch and Handoff are selected canonical work. Dependency blocking is derived;
  B3 deliberately permits independent manual lifecycle changes. Do not introduce
  an automatic launch gate, publish anything, or claim provider launch occurred.
- Completing work, approving output and uploading bytes do not implicitly complete
  parents or share private output. Client visibility is an explicit existing choice
  at Project, Deliverable and File boundaries. No credentials in handoff material.
- Keep immutable generation receipts while live work evolves; exact retries after
  delivery preserve statuses, timestamps, added handoff output, files and history.
- Current tenant/assignment/contact/visibility facts still govern every read,
  transition and byte download. Stale revisions cannot overwrite another decision.

## Acceptance

Durable integration tests exercise both blueprints with canonical domain mutations,
review/rework/approval/delivery, ordered launch/handoff dependencies, immutable
receipt replay, private/client-visible handoff bytes and current authority denial.
Stable local built Worker browser acceptance uses actual status and file upload
controls, exact downloadable bytes, explicit visibility, five widths and a Client
session. Use synthetic local D1/R2 and captured mail only. Existing broad tests
already cover individual lifecycle matrices and file faults; avoid duplicating them.

Run focused affected and full tests, syntax/diff checks and browser acceptance.
Reuse the verified D4 artifact when runtime/build inputs are unchanged and record
that fact. One fresh Sol High read-only review; at most one focused re-review.
Merge and verify both exact-SHA gates, live identity and disposable cleanup before
D6. No provider integration, production action, credential or migration.

## Local acceptance (2026-09-12)

Eight focused and 5,948 full tests pass. Actual Worker acceptance passes 149
browser checks / 72 captures across GHL and Kajabi at five widths, with exact
handoff bytes and no page errors. Final syntax/diff and guide links pass. No
runtime/build input changes; the verified D4 artifact was reused. One fresh Sol
High review found no material findings. Evidence and harness-only corrections:
[BUILD_STATE.md](../BUILD_STATE.md), `/tmp/bloomops-d5-handoff/`. Publication and
both exact-merge-SHA workflows remain pending.

## Closure (2026-09-12)

PR #49 merged as `a8b483aaf31ae26526dcda0f4068667c5c8bf465`, identical to the reviewed tree. Staging `34684310958` and zero `34684310988` passed every step; live identity and original eight-database inventory restoration passed. **D5 is CLOSED.** D6 follows.
