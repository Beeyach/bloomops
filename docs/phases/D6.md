# D6 — Systems Client Experience + Operations

Base: verified D5 merge `a8b483aaf31ae26526dcda0f4068667c5c8bf465`.
Continue until actual owner input is needed.

Verify generated GHL and Kajabi work in one multi-service workspace through the
existing portal Home's Your projects/Deliverables/Shared files sections and
internal Home, Work and Systems. No separate Systems portal module or invented
Projects/Files page. Refine only demonstrated gaps; reuse the existing exact DTOs.

## Acceptance

- Explicitly shared Milestones, outputs and ready handoff Files appear under the
  correct visible Project for its current Client contact. Internal QA, Actions,
  access details, assignments, descriptions, provenance and history stay hidden.
- Hidden-only children do not inflate portal or internal summaries. Both generated
  platforms retain the same allowlists and live canonical status/file truth.
- Issued Team sessions respect exact Project/Service/Client scope in Home, Work,
  Systems and direct File routes. Revocation removes rows, counts and historical
  names immediately. Contact unlinking removes both platforms and byte access.
- Exercise positive and denied real Worker HTTP/HTML/download paths, including
  child visibility changes, service and client assignment revocation, five widths,
  current sessions and mixed platforms. Synthetic local D1/R2/captured mail only.
- Retain meaningful mixed-platform integration tests, run focused/full suites,
  syntax/diff and browser checks. Runtime/build inputs unchanged may reuse the
  already verified D4 artifact; document any needed runtime correction/build.
- One fresh Sol High read-only review, at most one focused re-review; merge and
  verify both exact-SHA workflows, live identity and disposable cleanup before D7.

No schema, provider, credential, production or new lifecycle work is planned.

## Local acceptance (2026-09-12)

11 focused / 5,959 full tests, 104 Worker browser checks / 41 captures at five widths, and final syntax/diff checks pass. One fresh Sol High review found no material findings. The Work-tab harness correction and final evidence are recorded in BUILD_STATE and `/tmp/bloomops-d6-portal/`. Runtime/build inputs are unchanged; verified D4 artifact reused. Publication and both exact-SHA gates remain pending.
