# D4 — Systems Execution + QA

Base: verified D3 merge `689727c17547bfcc10e7b0741a8bb1e4d20b8856`; both
exact-SHA workflows, staging identity and disposable cleanup passed. Continue under the standing owner instruction until input is needed.

Make existing Systems project rows useful during execution by deriving a current
phase and action progress/blockers from readable canonical Milestones/Actions.
Reuse canonical Project/Action controls and Deliverable Internal Review for QA;
no competing Systems status, QA object, approval flow or provider action.

## Read model and UI

Opt-in execution fields on the existing Project summary query are requested only
by the Systems projection. Home/Work defaults and their DTOs remain unchanged.
Each child read keeps the existing live role/assignment/visibility predicate.
Preserve Systems eligibility, filters, pagination, attention sorting, timezone
and dependency-aware overdue rules; no hidden prerequisite IDs, names or counts.

- Current phase is the first readable in-progress milestone by position/id; if
  none, first waiting; otherwise first upcoming. Completed/skipped phases never
  become current. Closed Projects do not suggest a current phase even if earlier work remains unfinished. Show its canonical status and link to Project Milestones.
  This selects a useful phase to open, not a new exclusive lifecycle or inferred
  blueprint state. All-finished or no-readable-phase cases are described honestly.
- Action progress counts readable done/cancelled/open actions, without presenting
  cancellation as successful completion. Dependency-blocked count includes only
  readable open actions, using the existing prerequisite predicate. A hidden
  prerequisite may block a visible action, but its identity/count never escapes.
- Existing waiting, review, overdue, milestone progress, delivery review and file
  summaries remain canonical. Give direct links to Project Actions/Work waiting
  and review views and Project Deliverables/Internal Review. Do not infer QA or
  approval from a title or mark a build approved because QA actions are done.
- Keep the compact editorial Systems list and native accessible controls.
  No new page, general dashboard, state store, schema/migration or broad redesign.

## Acceptance

Focused fixtures cover both generated GHL/Kajabi and ordinary Systems projects,
phase ordering/transitions/skips, multiple active phases, all-finished/empty,
action completion/cancellation, waiting/review, visible and hidden dependency
blocking, hidden children/parents, cross-workspace and current assignment/member
revocation. Existing Home/Work/Systems regressions remain unchanged for default
callers. Bound page/query/parameter behavior on actual native D1 and retain the
accepted performance architecture; no new performance target or placement work.

Stable built-Worker browser story runs a generated Systems project through
canonical phase/action work and Deliverable Internal Review, verifies that the
Systems row follows those facts, opens the exact canonical control surfaces,
and proves restricted/Team/Client exclusion at five widths, keyboard and reduced
motion. Full tests, Cloudflare build and one fresh Sol High read-only review
(one focused re-review maximum). Both exact-merge-SHA workflows and staging
identity/cleanup precede closure. D5 Launch + Handoff follows; no Release E.

## Local acceptance (2026-09-12)

Implemented and independently reviewed with no material findings. 18 new /
158 affected / 5,940 full tests, 27 native D1 checks and Cloudflare build pass.
Native read retains five statements/one batch over 240 assignments, max 75 bindings
and 15,991 SQL bytes. Actual browser execution/QA/revocation passes 73 checks with
27 captures at five widths. No schema or provider changes. Evidence is under
`/tmp/bloomops-d4-execution/`; publication and exact-SHA gates remain pending.
