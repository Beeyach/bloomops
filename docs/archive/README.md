# Archived orphan documents

These documents were moved with `git mv` after an individual repository-wide filename search found no reference in another tracked file. Files and contents were preserved, not deleted. Subdirectories retain the original repository-relative layout beneath this directory.

Read a specific file only to resolve a named historical question. Do not read or enumerate all archives at session start. Routine ripgrep excludes this directory; explicit maintenance checks still include it.

Historical relative links and command paths retain their original context. For their original location, use the same suffix after `docs/archive/` at repository revision `7d8166cc317335c01d18fb8596908bcf4562b62d`. Do not execute historical commands merely because a report records them.

Four skill-source documents were retained in `skills/` because tests construct their filenames dynamically. README/license files, `.agents/`, entry instructions and referenced documents stayed in place.
