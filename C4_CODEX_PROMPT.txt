You are implementing BloomOps Release C, phase C4: Recordings + Content Assets.

Repository: Beeyach/bloomops
Branch: codex/c4-recordings-content-assets
Exact base: 6be5b4d668545d630c463e0a6b457ce7d53d6338
Model: Astra

Read these completely before changing code:

1. AGENTS.md
2. docs/BUILD_STATE.md
3. docs/RELEASE_C.md
4. docs/DOMAIN_MODEL.md
5. docs/PRODUCT_SPEC.md
6. docs/DESIGN_SYSTEM.md
7. docs/DESIGN_CHECKLIST.md
8. docs/phases/B5.md
9. docs/phases/C1.md
10. docs/phases/C2.md
11. docs/phases/C3.md
12. docs/phases/C4.md
13. current B5 File schema/domain/API/R2/download/UI/tests
14. current C1-C3 Content schema/access/domain/API/UI/tests
15. current Client portal shell and current migration/runtime/browser harnesses

Then execute docs/phases/C4.md exactly.

Non-negotiable scope:

- C4 only: recordings + Content assets.
- Extend the canonical B5 File foundation. Do not create a second storage system.
- Prefer an additive Content-specific fixed link table over weakening/rebuilding B5 asset_links into a speculative generic polymorphic model.
- Preserve B5 Project/Deliverable File behavior exactly unless a shared-helper refactor is required, and prove all B5 regressions.
- Preserve C1-C3 Content parent identity, assignment scope, visibility, pipeline legality, platform associations, target dates and calendar semantics.
- File operations must never auto-transition Content or alter Content dates/platforms/publication/parents.
- Client C4 access is only the minimum recording-request/upload/download path required by this phase. Do not build C6 general Content portal/navigation.
- A Client upload must be recording purpose only and require current contact linkage, Content client visibility, recording_required=true and stage=waiting_for_recording.
- Do not expose internal Content fields or Revision Requested context to Clients.
- Do not build C5 approval rounds/revision history, comments, notifications, templates, provider integrations, Release D+, production, DNS, real email, staging business-data mutations, or Leadsthatbloom changes.

Architecture requirements:

- Reuse assets + asset_upload_attempts + R2 bytes and the B5 reserve/write/finalize/recovery model.
- Keep R2 keys server-owned and opaque.
- Add a fixed same-workspace Content attachment with an exact small purpose vocabulary: recording and asset unless exact repository evidence requires a different naming.
- Enforce that one File cannot silently belong to both B5 Project/Deliverable and C4 Content attachment families.
- Keep Content attachment immutable after creation.
- Use current Content readability as the parent permission ceiling.
- Team Content File behavior may be broader than B5 Project File behavior only through a Content-specific action capped by the same C1-C3 Client/Service assignment rules. Do not loosen B5 file.manage globally.
- Client-uploaded recording visibility must be server-selected and client-safe.
- Internal visibility can remain internal/client/restricted and must intersect current Content scope.
- Successful Ready state must still require strong R2 evidence, one semantic FILE_UPLOADED event and strict generation fencing.
- Retry/recovery/archive/download must preserve B5 conservative D1/R2 failure behavior.
- Content/File history must filter old File facts by current File + Content readability.
- No public URLs, raw-key routes or storage authority leakage.

Recording-size requirement:

The existing assets table caps Files at 5 MiB and requires SHA-256. Do not casually raise it or buffer unbounded media. Preflight the exact current Worker/R2/runtime constraints and B5 code before deciding. If a larger safe recording protocol cannot reuse canonical assets without weakening invariants, retain 5 MiB for C4 and document the limitation. Do not invent unsafe pseudo-streaming, trust caller hashes, add third-party upload services or public presigned URLs just to make the number larger.

Migration:

- Inspect the exact journal first.
- Expected next domain migration is 0016_c4_content_assets.sql if inspection confirms it.
- Do not rewrite migrations 0012-0015.
- Prefer a small additive Content-link migration and only necessary indexes/triggers.
- If you believe assets itself must be rebuilt or its size CHECK changed, stop and prove why first. Preserve every existing B5 row/constraint/index/trigger/FK and zero-to-current guarantee.

Client surface:

- Keep it narrow and conditional.
- No general Social or Content client navigation unless unavoidable for the minimum recording request flow.
- Use an exact allowlisted DTO.
- Never expose owner, hook, script, caption, CTA, revision, internal activity, storage keys/hashes/leases/attempts, hidden counts or Revision Requested context.
- Treat C2 Waiting-for-Recording stage_context as internal unless you explicitly make only that context client-safe, document the decision and prove Revision Requested context can never leak.

Concurrency/failure requirements:

Prove all C4 requirements in docs/phases/C4.md, especially:

- identical upload retry convergence
- incompatible parent/purpose/details conflict
- attachment-family exclusivity
- immutable Content link
- atomic per-Content cap under concurrent uploads
- scope/contact/visibility/stage revocation between reserve, R2 put and finalize
- late old PUT/finalizer fencing after recovery
- D1 response-loss conservative cleanup
- visibility/archive/finalize races
- missing Ready object safe failure
- post-R2 download reauthorization
- no automatic C2 stage movement after recording upload

Verification:

Run the strongest current equivalents listed in docs/phases/C4.md. At minimum complete and report:

- npm ci
- focused C4 schema/domain/access/API/R2/UI tests
- B5 Files regressions and actual D1/R2 smoke
- C3, C2, C1 regressions
- B7 + B1-B6 regressions
- Release A/core + Client portal regressions
- full npm test
- npm run build
- npm run cf:build
- fresh/no-op zero-to-current
- actual workerd/D1/R2 C4 smoke
- prior C1-C3 and A/B runtime smokes
- built Worker HTTP
- local external verifier
- browser/HTTP C4 at 1440, 1024, 768, 390 and 320
- keyboard/focus/touch/reduced-motion
- current-session assignment/contact/visibility/stage revocation during byte operations
- changed JS/JSX syntax
- git diff --check
- dependency audit against exact C3 base. C3 currently has 53 advisories; prove any C4 delta rather than comparing only to the older 49 count.

Before opening the PR:

1. Update DOMAIN_MODEL.md and BUILD_STATE.md with exact implemented facts and evidence.
2. Update RELEASE_C/C3/C4 state only with claims actually proven.
3. Delete C4_CODEX_PROMPT.txt so it has zero net diff against base.
4. Ensure the working tree is clean.
5. Open a PR against main and leave it OPEN and UNMERGED for independent ChatGPT audit.
6. Include exact base SHA, final head SHA, changed-file inventory, migrations, test/runtime/browser evidence, dependency-audit comparison and any deliberate limitations in the PR body.
7. Do not merge the PR yourself.
8. Do not start C5.

C4 is not closed until independent audit passes, the user-controlled merge occurs, and both Deploy staging and Verify zero-to-current succeed on the exact C4 merge SHA.
