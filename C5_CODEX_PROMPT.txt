You are implementing BloomOps Release C, phase C5: Approvals + Revision History.

Repository: Beeyach/bloomops
Branch: codex/c5-approvals-revision-history
Exact base: 5b3e3c3ac3eae69b75310f73f9f54c7a85edc095
Model: Astra

Read these completely before changing code:

1. AGENTS.md
2. docs/BUILD_STATE.md
3. docs/RELEASE_C.md
4. docs/DOMAIN_MODEL.md
5. docs/PRODUCT_SPEC.md
6. docs/DESIGN_SYSTEM.md
7. docs/DESIGN_CHECKLIST.md
8. docs/phases/C1.md
9. docs/phases/C2.md
10. docs/phases/C3.md
11. docs/phases/C4.md
12. docs/phases/C5.md
13. current C1-C4 Content schema/domain/access/API/UI/activity/tests
14. current C2 stage transition engine and CAS/retry behavior
15. current C3 platform/date model
16. current C4 Content File + narrow Client recording routes
17. current Client contact/portal shell, migration, Worker/D1/R2 and browser harnesses

Then execute docs/phases/C5.md exactly.

Non-negotiable scope:

- C5 only: formal Content approval rounds + durable review revision history.
- Do not build C6 general Client Content portal/navigation, C7 hardening, notifications, comments, provider publishing, templates, Ads/Systems, production, DNS, real email, staging business-data mutations, or Leadsthatbloom changes.
- Approval history must be relational and append-only. Prior rounds and feedback are never overwritten.
- Each formal Client approval request must reference an immutable review revision that preserves exactly what was submitted for that round.
- Do not reuse content_items.revision as the durable review revision identity. It remains optimistic concurrency state.
- Activity is not the source of approval state.
- Do not add a second approval status onto content_items.
- Keep C2 stage as the only production pipeline state.

C4 closure is verified:

- exact merge SHA: 5b3e3c3ac3eae69b75310f73f9f54c7a85edc095
- Deploy staging 34333717208: success on that SHA
- Verify zero-to-current 34333717121: success on that SHA, including cleanup

Architecture direction:

- Inspect the exact migration journal first. Expected next migration is 0017_c5_content_approvals.sql if confirmed.
- Prefer additive Content-specific review revision + approval round tables with strong same-workspace/Content relations and immutable-history guarantees.
- Do not rewrite migrations 0013-0016.
- Do not rebuild content_items unless exact inspection proves a narrowly necessary constraint cannot be expressed additively.
- Review snapshots must use an exact allowlist. Do not serialize raw Content rows.
- Snapshot only fields explicitly needed for Client review. Preserve platform/date presentation needed to understand the submitted revision. Keep owner, internal ids, CAS revision, internal activity, hidden File/storage metadata, internal stage context and unrelated operational fields private.
- C4 remains the only Content File/R2 system. Do not create File versions or another storage system in C5.

Formal request behavior:

- Internal request is valid only for currently authorized Content that requires Client approval and is currently in client_review, with no other Requested round open.
- Capture the immutable review revision, allocate the next per-Content round number, create the Requested approval round and semantic activity atomically.
- Preserve Content stage as client_review when the approval is requested.
- Do not create snapshots on page render.
- Identical response-loss retry must converge without duplicate snapshot/round/activity.

Requested-round freeze:

- While a Requested round is active, reviewable Content fields represented in the snapshot, plus relevant platform/date mutations, must not silently change beneath the Client.
- Block/fence those mutations or use an equally strong design proven in tests.
- Do not rewrite the immutable snapshot after request.
- Visibility, assignment/contact and membership facts remain live authorization facts and may revoke access immediately.

Client response behavior:

- Client uses a dedicated narrow approval endpoint, never the generic C2 transition endpoint.
- Require current active Client membership, current contact linkage to the exact Content Client, Content client visibility, canonical optional Social service, Requested round, same Content/workspace revision, and current Content stage client_review.
- Approve: atomically mark round Approved, record responder/timestamp, move Content client_review -> approved using strict current-stage/current-revision fencing, and append semantic history exactly once.
- Changes Requested: require bounded non-empty feedback; atomically mark round Changes Requested, store immutable feedback, record responder/timestamp, move Content client_review -> revision_requested, set current C2 revision-request context from the bounded Client feedback where required, and append semantic history exactly once.
- Round feedback is durable history. stage_context remains current operational context only.
- Response-loss retry must converge. Competing approve/change/withdraw operations have one winner.

Coordinator behavior:

- Prefer Owner/Admin/Project Manager request/withdraw. Team may read approval history in current Content scope but should not request/withdraw formal Client approval unless exact product evidence clearly supports it.
- Withdraw preserves round + revision, marks only the Requested round Withdrawn, records who/when and optional bounded reason, and appends semantic activity.
- Prefer leaving Content in client_review after withdraw so editing can resume and a later request creates a fresh revision/round. If exact C2 rules require another safe behavior, document and prove it instead of inventing broad stage back-navigation.

C2 integration:

- Formal Client approval cannot be bypassed for client_approval_required=true.
- A Requested round must block competing stage transitions that invalidate the review.
- Direct internal client_review -> approved must not bypass the formal Client response after C5.
- Direct client_review -> revision_requested while a Requested round is open must not bypass the response/withdraw flow.
- Internal Review -> Revision Requested remains valid internal rework.
- Revision Requested -> Editing remains the C2 rework path.
- client_approval_required=false continues to skip Client Review exactly as C2 defines.
- Published remains terminal.

Client surface:

- Add only the minimum C5 approval surface, analogous to C4's narrow recording exception.
- Preferred shape: conditional Approval needed module on Client Home + dedicated /portal/approvals/[roundId] or equivalent.
- No general /portal/content or Social navigation.
- Exact allowlisted DTO from immutable revision snapshot.
- Approve and Request changes controls only.
- Hidden/ineligible rounds must not affect counts, overflow or empty states.
- Do not expose internal owner, membership ids, CAS revision, internal activity, private stage context, hidden counts, internal-only Files, R2 keys/hashes/leases/attempts, or raw service/department internals.

Concurrency requirements:

Prove all requirements in docs/phases/C5.md, especially:

- concurrent approval requests cannot allocate the same round number
- at most one Requested round per Content at a time
- request versus Content edit/platform/date edit has one canonical winner
- response versus response has one winner
- response versus withdraw has one winner
- response versus Content mutation/stage transition has one winner
- revocation of assignment/contact/visibility/membership with an issued session fences reads and writes immediately
- injected late failure rolls back revision + round + stage + activity atomically
- response-loss retries do not duplicate revision/round/activity/stage events
- later round never mutates prior revision or feedback

Revision-history semantics:

- Formal review history, not every keystroke.
- Each approval request freezes one immutable review revision.
- After Changes Requested or Withdrawn, internal users may revise live Content and later request a new round.
- New round gets a new immutable revision.
- Internal Content detail must show round/status/timestamps/feedback and the immutable submitted snapshot safely.

Verification:

Run the strongest current equivalents in docs/phases/C5.md. At minimum report:

- npm ci
- focused C5 schema/domain/access/HTTP/UI/concurrency tests
- C4 Content Files/R2 regressions and actual runtime smoke
- C3 Calendar/Platforms regressions
- C2 pipeline regressions, especially Client Review/Revision Requested/Approved behavior
- C1 Content regressions
- B7 + B1-B6 regressions
- Release A/core + Client portal regressions
- full npm test
- npm run build
- npm run cf:build
- fresh/no-op zero-to-current
- actual workerd/D1 C5 smoke
- prior C1-C4 and A/B runtime smokes
- built Worker HTTP
- local external verifier
- browser/HTTP C5 at 1440/1024/768/390/320
- keyboard/focus/touch/reduced-motion
- multi-round history with long copy/feedback
- issued-session revocation during request/response
- concurrent request/respond/withdraw/edit/platform/date races
- changed JS/JSX syntax
- git diff --check
- dependency audit against exact C4 base. C4 has 53 inherited advisories; prove any C5 delta.

Acceptance story must prove at least:

1. Content requiring Client approval reaches Client Review.
2. Coordinator requests round 1; one immutable review revision is created.
3. Linked Client alone sees actionable approval.
4. Reviewable live fields cannot silently change while Requested.
5. Client requests changes; round 1 remains immutable and Content becomes Revision Requested once.
6. Internal edit/rework returns to Client Review.
7. Round 2 freezes a new revision while round 1 and feedback remain unchanged.
8. Client approves round 2; Content becomes Approved once.
9. Retry/races/revocations are leak-safe and idempotent.
10. C3 calendar/platform and C4 File facts are not mutated by approval operations.
11. C6 general Content portal remains absent.

Before opening the PR:

1. Update DOMAIN_MODEL.md and BUILD_STATE.md with exact implemented facts/evidence.
2. Update RELEASE_C/C4/C5 state only with claims actually proven.
3. Delete C5_CODEX_PROMPT.txt so it has zero net diff against base.
4. Ensure working tree is clean.
5. Open a PR against main and leave it OPEN and UNMERGED for independent ChatGPT audit.
6. Include exact base SHA, final head SHA, changed-file inventory, migration details, test/runtime/browser evidence, dependency comparison and deliberate limitations.
7. Do not merge the PR yourself.
8. Do not start C6.

C5 is not closed until independent audit passes, the delegated/user-controlled merge occurs, and both Deploy staging and Verify zero-to-current succeed on the exact C5 merge SHA.
