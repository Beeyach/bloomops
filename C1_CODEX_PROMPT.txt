You are implementing BloomOps Release C, phase C1 Content Items Core.

Model: Astra.

Start with repository verification. Do not make implementation changes until you have confirmed all of these:

1. You are on branch `codex/c1-content-core`.
2. Its ancestry contains exact closed Release B main `c6509aa395a5db58310e2a0ae22a8a808082f77b`.
3. Remote `main` is still exactly `c6509aa395a5db58310e2a0ae22a8a808082f77b`. If main moved, stop and report before implementing.
4. Read `AGENTS.md`, `docs/BUILD_STATE.md`, `docs/ROADMAP.md`, `docs/RELEASE_C.md`, `docs/PRODUCT_SPEC.md`, `docs/DOMAIN_MODEL.md`, `docs/DESIGN_SYSTEM.md`, `docs/DESIGN_CHECKLIST.md`, `docs/phases/B7.md`, and `docs/phases/C1.md` completely.
5. Inspect the exact current Drizzle schema/journal/migrations, Client/Service composite keys, authorization/scope helpers, activity conventions, B7 exact HTTP input boundary, current Social placeholder, reusable BloomOps UI components, staging/zero workflows and strongest test/browser harnesses.

`docs/phases/C1.md` is the binding phase contract. If repository evidence reveals a small conflict, preserve security, tenant isolation, canonical parent ownership and Release C boundaries, choose the narrowest safe implementation, and document the choice. Do not silently broaden C1.

Release B is closed on `c6509aa395a5db58310e2a0ae22a8a808082f77b`. Update stale docs that still say B7/Release B is pending. Make Release C/C1 the current implementation state, but do not claim C1 complete until its local verification is actually green.

Implement C1 only:

- additive canonical Content Item schema, expected migration `0013_c1_content.sql` unless the journal proves another next number
- exact workspace + Client binding
- optional same-Client Service Engagement binding that must resolve canonically to the Social department
- structured title/type/pillar/owner/hook/script/caption/CTA fields
- exact workflow booleans `recording_required`, `internal_review_required`, `client_approval_required`
- target publish date
- canonical Release C stage vocabulary in storage, but C1 create must always start at Idea and no C1 API/UI may mutate stage
- visibility internal/client/restricted
- server revision, timestamps and creation retry identity
- no arbitrary caller workspace/client/stage/published authority
- no platform/calendar/recording/File/approval/revision-history/client-portal implementation in C1

Content types must be exactly the product vocabulary: Reel, Static Post, Carousel, Story, Video, Email, Ad Creative, Other. Persist normalized machine values with one canonical label/value module. Ad Creative is a type only, not Release D Ads behavior.

Authorization is load-bearing:

- Preserve Identity -> Role -> Scope -> Visibility and current database truth.
- Owner/Admin can coordinate Content across reachable workspace records.
- PM workspace coordination does not automatically grant restricted Content. Restricted PM/Team access requires an explicit current Client or Service assignment appropriate to that Content.
- Team Members are real Social fulfillment users and may create/edit/read Content only inside existing canonical Client/Service assignment scope.
- Service-specific Content must respect that exact same-Client Social service context. A caller must not attach Content to GHL, Kajabi, Ads or another non-Social service by ID.
- Client assignment may reach Client-level Content under the existing A/B scope semantics. Service assignment must not accidentally widen to unrelated Client services.
- Department membership, Content owner responsibility, Project assignment and Action-only assignment never grant Content scope.
- Client role gets no C1 internal Social access and no Content portal DTO/routes.
- Visibility `client` is only future eligibility. It creates no Client access in C1.
- All live reads and committing writes must re-check current workspace/membership/role/scope/visibility as appropriate.

Prefer immutable Client and Service parent binding after creation. Do not add a Content assignment table. Do not duplicate Client/Service/Department facts in mutable Content state beyond required FKs.

Creation/retry must follow the strongest current patterns:

- UUIDv4 request identity
- normalized immutable initial snapshot sufficient to distinguish identical response-loss retry from incompatible key reuse
- identical retry after later edits returns the same Content without reverting it
- concurrent identical requests create one fact and one semantic creation event
- incompatible reuse conflicts
- no retry can move workspace/Client/Service authority

Edits use revision CAS. Unknown JSON/query keys must reject the whole request, preserving the B7 authorization-before-validation leak-safety rule. Competing edits produce one canonical winner and one semantic history event. Injected late failure must roll back fact and activity together.

Use immutable significant activity. At minimum add canonical `CONTENT_CREATED` and one Content details/configuration update event unless the repository's existing event conventions justify a narrower split. Do not use activity metadata as mutable Content state.

Replace `app/(internal)/social/page.jsx` AreaPreview with a real internal Social Content list. Build a calm BloomOps-native create/detail/edit flow. Show useful current facts only. No fake pipeline board, calendar, recording uploader, approval panel, Client portal preview or Release D placeholders. Use existing design system/components rather than introducing a UI framework.

Content volume is long-lived. Use bounded deterministic reads and truthful overflow/pagination instead of an arbitrary small lifetime cap. Hidden rows must not cause false overflow. Keep relational scope predicates within D1 bind/statement limits. Do not materialize huge assignment ID lists.

Security/API requirements:

- real session identity
- active workspace membership
- Origin protection on mutations
- exact body/query allowlists
- malformed/null/array/scalar JSON safe 400
- unknown fields safe 400 with no partial mutation
- sanitized 400/401/403/404/409/500
- guessed/foreign/out-of-scope IDs leak-safe
- no raw SQL, stack, constraint or foreign record details
- protected responses no-store
- no weakening an authorization denial merely to validate input

Migration requirements:

- inspect for inherited name collisions before using `content_items`
- additive only
- no historical migration rewrite
- reuse canonical composite FKs/indexes
- add only indexes justified by C1 access/filter/order patterns
- fresh migration plus no-op second pass must remain identical/clean

Verification must be comprehensive. Run the strongest current equivalents of:

- `npm ci`
- focused C1 schema/domain/access/HTTP/UI tests
- B7 Release B hardening regressions
- B1/B2/B3/B4/B5/B6 focused regressions
- Release A/core auth/authorization/onboarding regressions
- full `npm test`
- `npm run build`
- `npm run cf:build`
- local fresh/no-op zero-to-current verifier
- actual disposable workerd/D1 C1 smoke
- prior actual D1/R2 Release A/B smokes
- built Worker HTTP verifier
- local external verifier
- C1 browser/HTTP acceptance at 1440, 1024, 768, 390 and 320
- keyboard/focus/touch/reduced-motion checks
- long title/editorial text, empty and overflow states
- current-session revocation/suspension tests
- large-scope/bind-limit tests
- changed JS/JSX syntax checks
- `git diff --check`
- `npm audit --json` comparison to the existing 49-advisory baseline

Acceptance must prove at least:

- multiple Clients with Social and non-Social Service Engagements
- valid Social-service Content and Client-level Social Content
- cross-Client/cross-workspace/non-Social service binding denied safely
- Owner/Admin/PM behavior
- Team Client/Service scoped read/create/edit
- restricted PM/Team explicit assignment behavior
- Department/owner/Project/Action non-grants
- stale issued session loses access immediately after scope/membership changes
- retry convergence/incompatible conflict
- edit/edit CAS race and atomic activity
- stage is always Idea and caller cannot mutate it in C1
- Client cannot access C1 Social internals or any Content portal projection
- Content reads do not mutate Release A/B parent lifecycles

Documentation:

- update `docs/BUILD_STATE.md` with Release B closure on exact SHA/gates and complete C1 implementation/verification evidence
- update `docs/DOMAIN_MODEL.md` from conceptual Content to the exact implemented C1 model
- update `docs/INDEX.md` to include `RELEASE_C.md`, current Release C/C1 state, and a C1 reading-matrix row
- update `docs/RELEASE_C.md` C1 state after implementation
- update other directly stale Release B/C1 references only where needed
- preserve historical evidence rather than rewriting it inaccurately

Before the final implementation commit:

- delete `C1_CODEX_PROMPT.txt`
- confirm it has no net diff against closed Release B main
- confirm working tree clean
- confirm no Release C phase C2+ implementation slipped in
- confirm no production, DNS, staging business data, real email or Leadsthatbloom changes

Then open a PR from `codex/c1-content-core` to `main`, leave it open and unmerged for independent ChatGPT audit.

Final report must include:

- exact base and final head SHA
- changed-file count/inventory by area
- migration name and schema summary
- exact Content field/type/stage/authorization choices
- test/build/D1/Worker/browser counts
- dependency advisory delta and limitations
- confirmation the temporary prompt is gone
- confirmation C2+ remains unimplemented
- confirmation independent audit, user-controlled merge, Deploy staging and Verify zero-to-current on the exact merge SHA remain required

Do not merge your own PR.