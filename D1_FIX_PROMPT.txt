D1 AUDIT FIX — SYSTEMS LOADING BOUNDARY / NO-JS ACCEPTANCE

You are correcting exactly one independently audited D1 blocker on PR #31. Work only on branch `codex/d1-systems-foundation`.

AUDITED BASE
- D1 base: `1638e3ed9fd33c3725aa3449935b08335b73f1a9`
- Audited D1 head that failed: `da8f2ec4633352b673aa62392a49700fdd234f4c`
- PR: #31

INDEPENDENT AUDIT RESULT
`AUDIT RESULT: FAIL - CHANGES REQUIRED`

BLOCKER
P2 — Systems loading boundary hides completed content and delays navigation.

The independent auditor reproduced that `app/(internal)/systems/loading.jsx` leaves the no-JavaScript `/systems` experience visibly stuck on “Loading Systems projects…” after the response completes, while the actual Projects/filters are present only in a hidden streamed segment. `scripts/systems-review-local.mjs` falsely passed because it counted hidden DOM nodes rather than proving visible completed content and did not actually submit the native filters.

The auditor also measured the loading boundary as an avoidable visible-navigation regression:
- Systems: ~345 ms median click-to-visible
- Work: ~79 ms
- Social: ~62 ms
- Systems with boundary removed experimentally: ~93 ms
- Original Systems network duration: ~36 ms

This is a D1-local blocker. Do NOT attempt to solve the separate staging-wide ~2 second navigation issue in this fix. A dedicated global performance-hardening phase will happen after D1 closes and before D2.

READ FIRST
Read completely before editing:
- `AGENTS.md`
- `docs/BUILD_STATE.md`
- `docs/RELEASE_D.md`
- `docs/phases/D1.md`
- `app/(internal)/systems/page.jsx`
- `app/(internal)/systems/loading.jsx`
- `app/(internal)/systems/error.jsx`
- `components/bloomops/SystemsOverview.jsx`
- `lib/bloomops/systems.mjs`
- `scripts/systems-review-local.mjs`
- `tests/bloomops-systems-page.test.mjs`
- relevant shell/navigation/browser harness code used by D1

Do not trust the implementation summary. Reproduce the blocker yourself before changing code.

REQUIRED FIX
Make the smallest root-cause correction that satisfies all of the following:

1. `/systems` with JavaScript disabled must visibly render the completed Systems page after the response completes. It must not remain visibly stuck on a loading fallback while the real page is hidden in a streamed segment.
2. Remove or restructure the route-level loading boundary if that is the root cause. Prefer deleting `app/(internal)/systems/loading.jsx` if repository/Next behavior confirms that is the smallest correct solution. Do not add fake client-side spinners, timeouts, hydration tricks, duplicate loading state, or a second data-fetch path.
3. Preserve the existing `error.jsx` recovery behavior and page orientation.
4. Preserve D1 authorization, relational Systems eligibility, pagination, filters, Work-Core links, bounds, and all other D1 behavior.
5. Do not weaken fresh per-request authorization or membership revocation semantics to gain speed.
6. Do not introduce schema, migrations, dependencies, infrastructure, D2 blueprint work, QA/launch features, Client Systems navigation, Ads, or unrelated refactors.

TEST CORRECTION
Update the D1 acceptance coverage so it proves real user-visible behavior instead of hidden DOM presence.

At minimum:
- Replace any test whose only purpose is asserting the removed loading component.
- In the browser harness, the no-JavaScript `/systems` check must require the actual completed Systems heading/content and project rows to be VISIBLE, not merely present in the DOM.
- The no-JavaScript path must actually submit at least one native Systems filter through normal browser form behavior and verify the resulting URL and visible filtered result(s).
- Ensure the loading fallback text is not the final visible state in the no-JavaScript probe.
- Keep all five viewport widths and existing accessibility/keyboard/touch/reduced-motion checks relevant to D1.
- Do not convert the browser test to JavaScript-enabled behavior to avoid the finding.

PERFORMANCE REGRESSION CHECK
Re-run the same local warm-navigation comparison, or the strongest reproducible equivalent available in the audit evidence, after the correction.

The goal of this D1 fix is not a global performance optimization. It is to remove the D1-introduced loading-boundary penalty. Record measured Systems warm click-to-visible behavior after the fix and compare it with Work/Social in `docs/BUILD_STATE.md`. If exact timing varies, report medians and methodology rather than claiming a hard SLA.

If Systems remains materially slower after the boundary is removed, investigate only enough to determine whether D1 itself still introduces a new regression. Do not expand into the broader staging latency project unless a D1-specific defect is proven.

VERIFICATION
Run the strongest current equivalents of the D1 verification suite, including:
- focused Systems tests
- Systems + shell tests
- relevant shared authorization/Service/B1-B7 regressions
- Release C regressions
- full `npm test`
- `npm run build`
- `npm run cf:build`
- local zero-to-current verifier
- schema/drift checks
- D1 Systems Worker smoke
- B6/B7/C7 runtime smokes
- built Worker auth smoke
- external verifier
- corrected Systems browser acceptance, including the independent no-JavaScript behavior and real native filter submission
- changed JS/JSX syntax checks
- `git diff --check`
- exact package/lock/schema/migration/Cloudflare config diff against D1 base

No skipped/todo tests. Do not weaken an existing test or production invariant to make the suite green.

DOCUMENTATION
Update `docs/BUILD_STATE.md` with:
- the independent audit failure
- root cause
- exact correction
- corrected no-JavaScript acceptance proof
- local performance comparison after correction
- verification evidence
- final corrected head SHA
- confirmation that the broader staging-wide ~2 second navigation problem is still deferred to the dedicated performance-hardening phase after D1 and before D2

Do not rewrite historical audit evidence as though the first audit passed.

FINAL REPO STATE
- Remove `D1_FIX_PROMPT.txt` before the final implementation commit so it has no net diff against main.
- Keep PR #31 open and unmerged.
- Push the corrected implementation branch.
- Working tree clean.
- Do not create or merge a second PR.
- Do not begin D2.

FINAL REPORT
Report concisely:
- exact previous failed head
- exact corrected head
- files changed by the correction
- root cause and fix
- corrected no-JavaScript/browser evidence
- warm-navigation measurements
- all verification results
- whether schema/dependencies/config remained unchanged
- any remaining uncertainty

Then stop for a fresh independent re-audit.