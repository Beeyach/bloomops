You are implementing PERF2 for Beeyach/bloomops.

START HERE

1. Confirm you are on branch `perf/perf2-staging-navigation`.
2. Read `AGENTS.md` completely.
3. Read `docs/phases/PERF2.md` completely. It is the binding contract.
4. Read `docs/PERF1_RESULTS.md`, `docs/phases/PERF1.md`, `docs/BUILD_STATE.md`, `docs/INDEX.md`, `docs/DOMAIN_MODEL.md`, and `docs/RELEASE_D.md` before editing.
5. Inspect the exact current implementation paths named in the PERF2 contract.
6. Confirm the PERF2 base is exactly `d0533cb9a7b3646134cbc194767af669c75f5adf`.

USER-OBSERVED BLOCKER

The user has personally retested the deployed PERF1 build on staging after both exact-SHA gates passed. Signed-in navigation is still noticeably slow even though the app currently contains very little real data. Treat this as a real product blocker. D2 must remain blocked.

PERF1 was useful but insufficient. It reduced total D1 calls, parallelized several reads, and preserved immediate next-request revocation. Do not undo it. The next question is remote round-trip depth, not merely raw query count.

IMPLEMENTATION METHOD

Work evidence-first.

A. Reproduce and instrument the critical path.

Extend the existing PERF1 harness so it distinguishes:
- total D1 statements/calls
- number of D1 binding invocations
- statements per invocation
- sequential D1 round-trip depth before completed visible content
- D1 wait versus non-D1 server time where safely measurable

Never log SQL, bind values, cookies, tokens, private content, or row payloads.

B. Inspect whether Cloudflare D1 native batching or equivalent safe query consolidation can reduce round-trip stages.

Pay special attention to:
- identity -> membership -> actor support
- actor capability / assignment / Client-link reads
- Home read model
- Clients counts/options/rows
- Work page reads
- Social options/list reads
- Systems projection reads
- Team reads
- Project detail metadata/body/options/child reads

Do not simply call Promise.all and assume it removes remote latency. Measure whether calls still cross the binding separately.

Where independent statements can safely run together, evaluate native D1 `batch()` or a similarly bounded request-local abstraction. If the app uses Drizzle paths that cannot batch directly, design the smallest safe adapter/fallback so production D1 gains the reduced remote invocation depth while local SQLite/tests remain deterministic. Keep authorization predicates explicit.

Do not combine unrelated data into giant unbounded queries. Do not widen authorization scope to simplify SQL.

C. Preserve security semantics exactly.

No cross-request caches. Better Auth cookie caching must remain disabled unless the binding contract's impossible-to-meet proof burden is satisfied, which is not expected here. Current membership, assignments, capabilities, visibility and Client links must still be re-evaluated on the next protected request.

Run explicit issued-session revocation tests after every structural optimization.

D. Measure scale, not just an empty app.

Create/reuse controlled local fixtures that meet the representative and stress sizes in `docs/phases/PERF2.md`. Keep UI pagination and bounds intact. Measure the seven required routes at both scales.

Track whether database size changes:
- critical-path round-trip depth
- SQL execution time
- payload/body completion
- browser click-to-visible

Use query plans if a hot query scales badly. Add an index only with measured evidence and only if necessary.

E. Obtain the strongest safe authenticated staging evidence possible.

The phase may not claim closure from localhost alone.

Reuse `scripts/navigation-perf.mjs` if possible. If an authenticated staging storage-state file is needed, support a safe one-time capture flow outside Git. Never print or commit the session. Never ask the user to paste a cookie or token into source/chat. Do not send a real magic-link email automatically.

If this CLI environment cannot complete interactive auth, leave a very short exact command/workflow the user can execute locally to capture the session securely, but continue all implementation and local/remote public investigation that does not need it.

F. Investigate placement only after application round-trip depth is minimized.

Do not change Cloudflare placement based on guesses. If remote evidence proves Worker/D1 geography is the remaining dominant latency, evaluate the smallest staging-safe placement option supported by the current stack. No production mutation.

PERFORMANCE TARGET

Aim for ordinary signed-in warm top-level navigation below 500 ms median and below 800 ms p95 where practical in the user's real staging path. Routine clicks should not commonly exceed 1 second.

More important than a synthetic target: materially eliminate the current slow feeling and prove the app remains bounded under representative data.

DO NOT CHEAT THE METRIC

Do not count first byte, shell paint, spinner, skeleton, optimistic placeholder, hidden streamed DOM, or early heading as completion. Measure the actual completed destination content.

Do not reintroduce a route loading boundary that breaks no-JavaScript completed visibility.

VERIFICATION

Run every verification family required in `docs/phases/PERF2.md`, including full tests, builds, runtime smokes, browser acceptance, zero-to-current, security/revocation regressions, representative/stress performance runs, syntax and diff checks.

Before final handoff:
- write `docs/PERF2_RESULTS.md`
- update `docs/BUILD_STATE.md` and `docs/INDEX.md`
- update durable docs only if warranted
- remove `PERF2_CODEX_PROMPT.txt`
- ensure the working tree is clean
- push `perf/perf2-staging-navigation`
- open a PR against `main`
- leave it OPEN and UNMERGED for independent audit

FINAL REPORT FORMAT

Give the user a concise but complete report containing:
1. exact base SHA
2. exact final head SHA
3. PR number and state
4. reproduced root cause(s)
5. D1 total calls AND sequential remote round-trip depth before -> after per route
6. exact code/architecture changes
7. representative and stress dataset sizes
8. before/after timing tables
9. authenticated staging evidence actually obtained, clearly distinguished from simulations/local tests
10. security/revocation results
11. full verification summary
12. schema/dependency/infrastructure delta
13. remaining uncertainty
14. explicit statement that D2 remains blocked until independent audit, merge, exact-SHA gates, and user staging retest pass

Do not merge the PR yourself.
